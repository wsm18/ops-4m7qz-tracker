Paste this into a new Claude Code session to resume work.

---

You are continuing work on **Operations**, a gamified ROTC life-tracker PWA built for an Army ROTC cadet (Wyatt, MS2, Cyber branch goal). **Read all of these before touching any code:**

1. `CLAUDE.md` — the binding rulebook (hard rules, workflow, file layout)
2. `planning/FINISHED-FEATURES.md` — design language, color palette, completed features, project identity
3. `planning/IMPROVEMENTS-skills-expansion.md` — the comprehensive skills backlog; consult whenever adding skills so you don't duplicate effort or miss obvious gaps

**Current version: v165.** The service worker is at `operations-v165` in `sw.js`. `SKILL_LADDER_VER` is currently **117** (in `src/core/migration.js` — unchanged since v155; v156 through v165 didn't touch it). Total skills: **11274** (10649 + 625 from v165's new tree) — crossed the 11,000-skill mark this session.

**v157 found a real error in the previous version of this file** — read it before trusting any "N trees remain" claim, including this one, without re-deriving it yourself. See "Pyramid state" below. v158 through v165 all re-derived the count fresh at session start and confirmed it matched exactly (no drift) — still worth re-deriving yourself every session rather than trusting this number once enough time has passed.

**IMPORTANT: commit this work to git before starting anything new.** v153 and v154 both sat uncommitted in the working tree for an entire session each — this is exactly what turned a routine file-corruption incident during v154 into a multi-hour recovery instead of a trivial `git checkout`. Confirm `git status` is clean (or at least that `src/core/skills-data.js` and `sw.js` are committed) before dispatching any new agent wave.

There is no `IMPROVEMENTS-vNNN.md` file right now. **Read the v148 through v157 entries in `FINISHED-FEATURES.md` before doing any pyramid-completeness auditing or Commons-layer work** — they document real mistakes from prior sessions:
- v148: miscounting set membership by filtering out Joker/auto skills, which led to writing and then fully reverting 54 unnecessary skills. Lesson: **when checking whether a pyramid set is complete, count ALL members of its `setKey` (matching `skSetMembers()`'s own rarity-agnostic definition), never a rarity-filtered subset.**
- v149: a fix script that matched skills by `name` alone patched the wrong object twice, because two skill names (`Penetration testing methodology`, `Legal literacy`) are pre-existing duplicates within the same `cat`. Both were caught and corrected. Lesson: **any script-based fix keyed on `name` must also check `cat` (and ideally `setKey`) before assuming a match is unambiguous** — see the still-open list of 10 pre-existing name+cat duplicates in the v148 entry.
- v150: a large parallel-agent dispatch (25 agents) hit an account-wide session-limit cutoff partway through, leaving the file syntactically broken (one missing comma) and several setKeys partially written. Lesson: **after any large parallel dispatch, don't trust the agents' own success reports at face value — run `node --check` and the marker/member-count sweep yourself before proceeding**, the same discipline as the rest of the ship checklist, but now also as a first-response check for a broken mid-dispatch state. Also: two agents in different dispatch waves independently chose the same skill names for unrelated setKeys — concurrent bulk-writing carries a collision risk against **sibling agents' output**, not just the pre-existing file, so the final duplicate sweep is load-bearing even when every individual agent claims it pre-checked.
- v151: picked up mid-interruption from a prior v151 attempt (7 leftover unapplied agent-output files + a partially-written tree). Two of those leftover files themselves had only generated 4/5 Commons for 6 setKeys before being cut off — a defect that looked identical in quality/format to the complete files and would have shipped short without a full member-count sweep on *inherited* content, not just newly-generated content. Lesson: **when recovering from an interruption, don't assume a leftover/inherited file is internally complete just because it parses cleanly and its setKeys don't collide with anything else — count members per setKey regardless of where the content came from.** Also switched to smaller sequential dispatch waves (4 agents/wave instead of one 25-agent wave) specifically to bound the blast radius of another possible mid-dispatch cutoff, and it worked without incident.
- v152: no interruption this session — verifying after every wave (not just at the end) caught a real defect early: one agent wrote all 25 of its assigned skills correctly but left 4 stale marker comment lines in place instead of deleting them (harmless to parsing/counts, but sloppy and confusing on a later grep). Lesson: **after every wave, don't just check member counts — also grep for the exact expected reduction in leftover `SLOT:` marker count.** A mismatch there catches "wrote the content but didn't clean up the marker" mistakes that a pure member-count sweep won't flag (the skills exist and count correctly either way). Also: several agents self-caught their own dropped skills (wrote 4/5 instead of 5/5) during their own end-of-task verification before reporting done — a good sign the per-agent verification instructions are working, but don't rely on it exclusively; the after-every-wave sweep is still what actually confirms it.
- v153: two real defects caught by verifying after every wave. (1) Two sibling agents dispatched in the *same* wave independently picked the identical skill name ("Position of Attention Precision") for two different setKeys — neither agent's own pre-write grep could catch this since each only checked history that existed *before* the wave started, not a sibling's simultaneous output. Caught by the routine whole-file name+cat duplicate sweep (11 dupes instead of the expected 10), fixed by renaming one. (2) Far more serious: while investigating that collision, one agent's skill was found to be missing its `howTo` field — and it turned out the *same agent* had omitted `howTo` from all 25 of its 25 skills, despite the task prompt showing the exact required shape. **A pure member-count sweep never would have caught this** — every setKey still had exactly 5 members. It only surfaced because a new full required-field validator (checking every field's presence, array lengths, rarity, fadeDays, and absence of stray fields) was written and run for the first time. Lesson: **member-count + marker-count sweeps are necessary but not sufficient. Run a full required-field/array-length validator after every wave too** — a structurally-complete-looking set of 5 skills can still individually be missing required content.
- v154: **discovered v153 had never been committed to git** — it existed only in the uncommitted working tree, with zero git-level safety net, right alongside v154's own uncommitted work. This is exactly why the incident below required a manual OneDrive recovery instead of `git checkout`. **Two real file-corruption incidents this session**, both from concurrent agents editing the shared `skills-data.js`: (1) a script fallback used Python's `open(path,'w')` — a truncating write mode that zeros the file the instant it opens — which raced against another agent and then failed before writing replacement content, leaving the file at 0 bytes; recovered via OneDrive version history (a snapshot from ~23 minutes earlier, verified byte-for-byte clean before trusting it). (2) Even after switching to a safer "read fresh → write to temp → atomic rename" pattern, a **lost-update race** still silently reverted one already-completed setKey back to just its marker comment — atomic rename prevents a file being observed in a *truncated* state, but does NOT prevent one agent's full-file write (computed from a stale read) from clobbering another agent's concurrent edit that landed in between. Caught by the routine post-wave member-count sweep, fixed by hand-authoring the 5 skills directly. **Lesson: prefer the Edit tool (real staleness detection against the live file) over any script-based fallback, and if a script is truly unavoidable, never batch multiple markers into one read-modify-write-all cycle — read fresh immediately before each individual write.** After switching to Edit-tool-first + reduced 2-agent-per-wave concurrency from wave 5 onward, zero further incidents. **Also: commit uncommitted pyramid work to git at the end of every session** — don't let it sit uncommitted like v153 and v154 did.
- v155: a validator introduced partway through a workstream (v153's required-field validator) doesn't retroactively apply itself. Running it retroactively against v151/v152 found real, previously-shipped defects (array-length violations, missing `howTo` fields) that had gone undetected. **Lesson: when you introduce a new check, consider running it once against everything that predates it, not just new work going forward.**
- v157: **the resume doc itself was found stale and wrong.** The prior version of this file claimed only 5 Mythic trees remained for the Commons layer, when the real number (re-derived directly from `SEED_SKILLS` via a Node script walking all 16 Mythic trees down to their Commons layer) was **11** — the doc's own "16 Mythic trees + 6 second-gen Phase 7 trees" framing implied the 6 second-gen trees still needed Commons, but its "what's next" summary only named the 5 legacy-gen ones. **Lesson: this project's own "don't trust an inherited doc" rule applies to `NEXT-SESSION-PROMPT.md` itself, not just audit docs — always re-derive the remaining-tree count from `SEED_SKILLS` before picking a tree, every session, no exceptions.** Also found and fixed a new failure mode: a missing trailing comma at a wave-to-wave group boundary (the last skill object of one Rare group not comma-terminated before the next group's comment header) broke `node --check` — invisible to a naive "did each wave produce 25 valid-looking skills" check, only caught because `node --check` runs after every wave, not just at the end. **Lesson: when instructing agents to insert content ending right before another group's comment/marker, explicitly warn about the trailing-comma boundary — it's an easy thing for an agent editing only its own 5 setKeys to overlook, since the comma requirement depends on content outside its assigned scope.**
- v158: this session re-derived the count fresh and it matched the v157 doc exactly (6274 total, 6 trees at 625 Commons, 10 at 0) — no drift this time, but still worth doing every session since drift is only detectable by actually checking. New failure mode found: several agents across different waves independently hit a **transient marker-duplication artifact** under heavy concurrent editing — a `// SLOT:...` comment line briefly appearing doubled after a neighboring agent's edit landed nearby (looked like a race in how concurrent Edit-tool writes interleaved near-adjacent lines, not a logic error in any single agent). Every occurrence was self-caught by the standing "re-grep for your remaining markers after every single Edit call" instruction already in agent prompts, and folded into a clean single replacement before the agent reported done — a whole-file `grep "// SLOT:" | sort | uniq -d` at the end of every wave confirmed zero survived into the committed file. **Lesson: the instruction to re-grep after every Edit call (already standard practice) is doing real work here — keep it in every agent prompt verbatim, and add an explicit end-of-wave `sort | uniq -d` sweep for literal duplicate marker lines to the standard checklist** (see step 7 below), not just the marker-count-dropped-by-N check, since a marker that's merely *duplicated* (not missing) won't show up in a raw count.
- v159: re-derived the count fresh again, matched v158's doc exactly (6899 total, 7 trees at 625 Commons, 9 at 0) — no drift. Completed **Sovereign Self** (`cat:"personal"`), deliberately picked from the collision-flagged cats rather than avoided. Two new defects found this session, **both caught by the orchestrating session itself, not by any dispatched agent** — an important distinction from every prior session's incidents, which were all self-caught by agents or caught by the post-wave sweep script. (1) A marker for one setKey got corrupted mid-session — its `// SLOT:...` line was split across two physical lines by a concurrent-edit artifact, sitting in a group that hadn't been dispatched yet. It was noticed and flagged by an *adjacent* group's agent as out-of-scope, then fixed directly by the orchestrator before the affected group's agent was ever dispatched. (2) A duplicated `// ── GROUP N ──` header comment line was found sitting in undispatched territory during a routine post-wave grep sweep — no agent had reported it because no agent had looked at that group yet. **Lesson: the after-every-wave verification sweep needs to scan for corruption in *undispatched* groups' markers/headers too, not just the just-completed wave's own content** — a defect sitting in territory nobody has touched yet is invisible to any per-wave "did my own markers survive" check, and will otherwise get silently handed to whichever agent is dispatched next as if it were normal marker text.
- v160: re-derived the count fresh again, matched v159's doc exactly (7524 total, 8 trees at 625 Commons, 8 at 0) — no drift. Completed **The Living Root** (`cat:"roots"`), the last of the original legacy-gen 16 Mythics and the lowest-collision-risk of the 8 remaining trees (zero pre-existing name+cat dupes in `roots`, unlike `technical`/`cognitive`/`personal`). One pre-existing, unrelated defect found via the routine duplicate-header sweep and fixed on sight: a literal duplicated `// ── GROUP 8 | Leg: Army Fitness Excellence | Rare: Field Physical Readiness ──` header line sitting in the already-shipped Soldier Athlete tree from a prior session — harmless to parsing/counts, but confirms the "scan the whole file, not just your own wave" discipline from v159 catches real cruft anywhere, not only in currently-active work. Several agents this session self-caught and fixed their own mid-task slips (a duplicated header, a stray marker/comma artifact, a 4/5 undercount) via the standing re-grep/re-count discipline before reporting done — none reached the committed file uncaught, an encouraging sign the per-agent instructions are doing real work, though the orchestrator's own independent sweep is still what actually confirms it.
- v161: re-derived the count fresh again, matched v160's doc exactly (8149 total, 9 trees at 625 Commons, 7 at 0) — no drift. Completed **Warrior Foundation** (`cat:"tactical"`), the first second-gen Phase 7 tree to get its Commons layer and the lowest-collision-risk of the 7 remaining trees per the then-known 10-item dupe list. **Found a new failure mode not seen in any of the 9 prior trees:** wave 1 introduced 3 name+cat collisions not against the known 10 pre-existing dupes, but against the ALREADY-SHIPPED legacy "Tactical Mastery" Mythic tree's Commons — a sibling tree sharing the same `cat:"tactical"` from a much earlier session. Generic military-training names happened to already exist in that other 625-skill tree. Caught immediately by the routine post-wave duplicate sweep, fixed by renaming the 3 new skills. **Lesson: when two Mythic trees share a `cat`, the collision risk isn't limited to the fixed 10-item pre-existing list — it also includes every skill already shipped by that cat's other tree(s).** From wave 2 onward, every agent prompt required a live `grep '{name:"<candidate>", cat:"<cat>"' ` check per name before writing, which then self-caught several more near-misses (groups 3, 11, 13, 15, 16, 21, 24) before they were ever written — zero further collisions reached the committed file after the fix.
- v162: re-derived the count fresh again, matched v161's doc exactly (8774 total, 10 trees at 625 Commons, 6 at 0) — no drift. Completed **Master of the Mind** (`cat:"cognitive"`), the second second-gen Phase 7 tree, picked specifically because `cognitive` carries only 1 known pre-existing dupe AND (unlike `leadership`) has zero same-cat-sibling risk since neither `cognitive` Mythic tree had shipped yet. **One real defect caught by the routine post-wave sweep, not by the dispatched agent's own report:** wave 5's agent confidently reported all 5 setKeys complete at 5 members each, but the independent grep-count sweep found `cog_c_proc_interference` and `cog_c_skill_transfer` both at 4/5 — fixed by hand-authoring the missing skill for each rather than redispatching. **Lesson (reinforcing v152/v153): a confident-sounding agent self-report is not evidence — only the orchestrator's own independent count is.** Two markers had pre-existing name/setKey label mismatches baked into the underlying data (`cog_c_decision_sci` labeled "Decision science", `cog_c_spatial_reasoning` labeled "Spatial reasoning" — both actually belonged to different Rares than their label suggested); both were flagged explicitly in the relevant agent prompts and handled correctly by writing to the setKey's real thematic slot rather than the misleading label.
- v163: picked up a **staged** state left by the immediately-preceding session — 125 `// SLOT:` markers already pre-placed for Cyberspace Operations Officer (`cat:"technical"`), doc'd in a since-deleted `IMPROVEMENTS-v163.md`, committed as `v163 (staged)`. Re-verified the staged state before trusting it (markers intact, `node --check` clean, pyramid state re-derived matching the staging doc exactly — 9399 total, 11 trees done, 5 at 0). Completed **Cyberspace Operations Officer**, the third second-gen Phase 7 tree and `technical` cat's first tree done. 13 waves, 2-agent concurrency throughout. **Zero defects found in any wave — a first for this workstream** (every prior tree from v150 onward had at least one self-caught or orchestrator-caught defect). Made 3 intermediate safety commits at Legendary-boundary checkpoints (after waves 5, 8, 10) instead of holding all 625 skills uncommitted for the whole session. The highest-scrutiny area (`technical`'s 2 known pre-existing dupes — "Penetration testing methodology" and "Cyber Operator", both landing in the "Cyber Operator" Legendary, waves 6-8) held clean under explicit per-name whole-file grep discipline. Crossed 10,000 total skills this session. **Lesson: staging markers in a separate session before executing them (rather than doing both in one session) worked cleanly** — the staging session's own re-derivation and the executing session's re-verification of that staging both matched with no drift, suggesting this split is safe to repeat if a session runs low on room to both stage and fully execute a tree.
- v164: a direct continuation of the v163 session in the same conversation (user said "begin the next skill group" rather than opening a fresh session from this doc) — re-derived the pyramid state fresh anyway per standing discipline (10024 total, 12 done, 4 at 0), matched exactly. Completed **Staff Excellence** (`cat:"leadership"`), the fourth second-gen Phase 7 tree, picked as the only remaining tree with 0 known pre-existing dupes even though — as of this session, true for ALL 4 remaining trees — it has a shipped same-cat sibling (Battlefield Commander). Mapped the tree's structure directly from `SEED_SKILLS` and ran a v149-style prerequisite check before staging 125 markers; found a benign legacy-prefix quirk (5 setKeys under the first Legendary use the OLD `lead_c_*` prefix, not this tree's own `lead2_c_*`, because those 5 Uncommons are pre-existing legacy skills wired in per the "existing skill integration rule" — verified genuinely unclaimed before proceeding, not a defect). 13 waves, 2-agent concurrency throughout. **One real interruption:** wave 8's group-15 agent hit a mid-task API connection failure before writing anything — confirmed zero-write via `node --check` + untouched markers, redispatched cleanly. **One new failure-mode nuance found:** an independent post-wave count briefly read a setKey at 4/5 while a concurrent sibling agent was still mid-write on its own self-caught fix — resolved on re-check seconds later, no actual defect. **Lesson: a shortfall found by the orchestrator's own count isn't automatically confirmed either — if the owning agent hasn't reported done yet, re-check after a brief pause before treating it as real**, the mirror image of the standing "don't trust agent self-report" lesson. Added a "re-count as your last action, after a pause" instruction to every subsequent agent prompt. 3 intermediate checkpoint commits at Legendary boundaries. Crossed 10,500 total skills this session.
- v165: another direct continuation in the same conversation ("begin the next commons layer") — re-derived fresh anyway (10649 total, 13 done, 3 at 0), matched exactly. Completed **Cognitive Athlete** (`cat:"cognitive"`), the fifth second-gen Phase 7 tree, picked for lowest known-dupe-count (1, "Cognitive Resilience" — confirmed as a pre-existing unrelated Rare-vs-Legendary-name collision, not new). Same benign legacy-prefix quirk as v164 (5 setKeys under the first Legendary using the old `cog_c_*` prefix from pre-existing legacy skills) — verified unclaimed, not a defect. 13 waves, 2-agent concurrency. **One clean interruption, same failure mode as v164's:** wave 9's group-17 agent hit a mid-task API server error before writing anything — confirmed zero-write, redispatched cleanly. This is now 2 sessions in a row with this exact pattern; treat a mid-dispatch API failure as a routine, expected event to budget for, not a rare exception. **New finding: this tree had unusually high WITHIN-tree thematic density** (fatigue/load/offloading/reframing/pattern-recognition concepts clustering tightly across many groups, not just against the sibling Master of the Mind tree) — 6+ groups needed explicit "distinct from Group N's X, which covers Y specifically" differentiation language in the agent prompt to avoid near-miss overlaps; held clean throughout. **Lesson: before writing agent prompts, check whether the tree's OWN groups cluster thematically (fatigue, load, teaching, offloading, etc. all recur across cognitive-domain trees) — a generic "don't duplicate the sibling tree" warning is not precise enough at high internal density; name the specific sibling group and the scope boundary explicitly.** 3 intermediate checkpoint commits at Legendary boundaries. Crossed 11,000 total skills this session.

The legacy pyramid trees (cognitive, physiological, technical, academic, personal, hearth, roots) are, as of right now, verified to be a genuinely clean 5/5/5 across all 80 Legendaries and every Rare/Joker slot-holder's Uncommon set — 400 total slot-holders, 2000 total Uncommons, zero gaps anywhere. Every one of those 2000 Uncommons has its own unique, unshared Commons-tier placeholder key (0 orphans, 0 collisions, verified as of v149). **As of v165, fourteen of 16 Mythic trees' worth of Commons content — Physical Mastery, Keeper of the Flame, Tactical Mastery, Battlefield Commander, Vital Operator, Scholar-Warrior, Soldier Athlete, Sovereign Self, The Living Root, Warrior Foundation, Master of the Mind, Cyberspace Operations Officer, Staff Excellence, and Cognitive Athlete, 8750 skills across 1750 setKeys — are written and verified**. The 2 remaining trees (Cyber Operator, Life Architect) are all still at 0 Commons — this was confirmed directly from `SEED_SKILLS`, not inferred from a "legacy vs. second-gen" label, which past sessions found unreliable to reason about without re-deriving. Don't re-litigate any of this without re-deriving the count yourself first.

---

## What's already done

Full history is in `planning/FINISHED-FEATURES.md`. Do not re-implement anything listed there.

### Pyramid state (the active multi-session workstream)

The app has a card-game pyramid system where skills form a 5-tier synthesis chain: Common → Uncommon → Rare → Legendary → Mythic.

**Every Mythic tree — both generations, all 16 of them — is a clean 5/5/5 through the Uncommon layer, verified.** 16 Mythics × 5 Legendaries = 80 Legendaries, each with exactly 5 total slot-holder members (Rare-tier or, in several legacy sets, a pre-existing Joker/auto skill occupying a legitimate slot) = 400 slot-holders, and every slot-holder's Uncommon-tier feeder set has exactly 5 members = 2000 Uncommons. This was checked with a Node script that loads `skills-data.js` as a module and replicates `skRarity()` exactly (see the v148 `FINISHED-FEATURES.md` entry for the script's logic) — **do this yourself before trusting any inherited claim about missing Rares/Uncommons**, including claims in this very file if enough time has passed that drift is possible.

**Two auditing traps to avoid, both hit in the v148 session (see `FINISHED-FEATURES.md` for the full story):**
1. **A written gap-list doc can go stale.** If work happens between when an audit doc is written and when you act on it, re-derive counts from `SEED_SKILLS` directly rather than trusting the doc's numbers. (v157 found this applies to `NEXT-SESSION-PROMPT.md` itself — see above.)
2. **Counting "true Rare" (rarity-filtered) members is not the same as counting "total slot-holder" (rarity-agnostic) members**, and only the second one matches `skSetMembers()`/`skSetCanCombine()`'s actual behavior and the project's "exactly 5 members" rule. Several legacy Legendary sets have 1-2 pre-existing Joker/auto skills (e.g. `Reaction speed`, `Resting heart rate`) legitimately occupying real slots alongside 3-4 authored Rare cards — filtering those out before counting makes a complete set look short.

**Session sequence:**
- v144: misc deck elimination + Soldier Athlete Mythic ✓ done
- v145: pyramid structural repair (190 Uncommons + 1 bug fix) ✓ done
- v146: Phase 7 structure — 5 new Mythics + 20 new Legendaries + 100 new Rares ✓ done
- v147: Phase 7 Uncommons (500 skills) + storage refactor (fixed a real quota crash) ✓ done
- v148: investigated a claimed legacy-pyramid gap, found it didn't exist. Net change: none. Still at v147/2524 skills.
- v149: Commons-layer prerequisite — fixed 6 orphaned Uncommons and split 10 collision groups so every one of the 2000 Uncommons has a unique Commons-tier placeholder key. 2524 unchanged.
- v150: Commons layer, first tree — **Physical Mastery**, 625 skills. 2524 → **3149**.
- v151: Commons layer, second tree — **Keeper of the Flame**, 625 skills. 3149 → **3774**.
- v152: Commons layer, third tree — **Tactical Mastery**, 625 skills. 3774 → **4399**.
- v153: Commons layer, fourth tree — **Battlefield Commander**, 625 skills. 4399 → **5024**.
- v154: Commons layer, fifth tree — **Vital Operator**, 625 skills, survived two file-corruption incidents. 5024 → **5649**.
- v155: retroactive audit — ran the v153 required-field validator against v151/v152 for the first time, found and fixed real pre-existing defects. 5649 unchanged, `SKILL_LADDER_VER` → 117.
- v156: FM training plan content fix (warm-up/cool-down stretches, `SESSIONS` not `SEED_SKILLS`) — unrelated to the pyramid workstream. 5649 unchanged.
- v157: Commons layer, sixth tree — **Scholar-Warrior**, 625 skills. Also corrected the remaining-tree count from a stale "5" to the real 11. 5649 → **6274**.
- v158: Commons layer, seventh tree — **Soldier Athlete**, 625 skills, zero file-corruption incidents (2-agent-per-wave discipline holding across 4 trees straight now). 6274 → **6899**.
- v159: Commons layer, eighth tree — **Sovereign Self**, 625 skills, zero file-corruption incidents (5 trees straight now at 2-agent concurrency). Orchestrator caught two corruption defects sitting in undispatched territory before any agent touched them. 6899 → **7524**.
- v160: Commons layer, ninth tree — **The Living Root**, 625 skills, zero file-corruption incidents (6 trees straight now at 2-agent concurrency). Last of the legacy-gen 16 Mythics done; all 7 remaining trees are second-gen Phase 7. 7524 → **8149**.
- v161: Commons layer, tenth tree — **Warrior Foundation**, 625 skills, zero file-corruption incidents (7 trees straight now at 2-agent concurrency). First second-gen Phase 7 tree done. Found and fixed a new cross-tree same-cat name-collision failure mode (see above). 8149 → **8774**.
- v162: Commons layer, eleventh tree — **Master of the Mind**, 625 skills, zero file-corruption incidents (8 trees straight now at 2-agent concurrency). Second second-gen Phase 7 tree done. Caught one real 4/5 undercount via the post-wave sweep despite a confident agent self-report (see above). 8774 → **9399**.
- v163: Commons layer, twelfth tree — **Cyberspace Operations Officer**, 625 skills, zero file-corruption incidents (9 trees straight now at 2-agent concurrency) AND zero defects of any kind in any wave, a first for this workstream. Third second-gen Phase 7 tree done; `technical` cat's first tree done. Resumed from a staged-marker state left by the prior session. Crossed 10,000 total skills. 9399 → **10024**.
- v164: Commons layer, thirteenth tree — **Staff Excellence**, 625 skills, one clean interruption-recovery (an agent's connection failure before it wrote anything, redispatched with no incident) and zero true defects (one transient false-alarm from a read racing a concurrent write, resolved on re-check). Fourth second-gen Phase 7 tree done. Crossed 10,500 total skills. 10024 → **10649**.
- v165: Commons layer, fourteenth tree — **Cognitive Athlete**, 625 skills, one clean interruption-recovery (an agent's server-error failure before it wrote anything, redispatched with no incident — 2nd session in a row with this exact pattern). Fifth second-gen Phase 7 tree done. Discovered this tree's unusually high within-tree thematic density required extra differentiation language in agent prompts (see lessons above). Crossed 11,000 total skills. 10649 → **11274**.
- **Next: the Commons layer for the remaining 2 Mythic trees** (1,250 cards, both second-gen Phase 7) — nearly 90% done, one proven-working process, repeat it — see below

**After the Commons layer (all 16 Mythic trees) is fully complete:** the next workstream is queued in `planning/IDEAS-tests-fm-workouts.md` — a fully-designed, Wyatt-confirmed set of FM/test features (gym-schedule-aware training planning, equipment-aware exercise selection, stealth-assessment cognitive/quiz games, card-game workouts) plus four additional green-lit novel ideas (a unified cross-tab deadline timeline, an AAR-style reflection journal, a cross-domain data-insight engine, and a whole-tree "smart focus" recommender), with a recommended 8-phase build order (X-Timeline → X-AAR → FM-1 → FM-2 → X-Insight → T → FM-3 → X-SmartFocus) at the bottom of that doc. Read it before starting that work; don't re-derive the design from scratch.

---

## What's next: continue writing Commons content, tree by tree

The prerequisite is done, the object shape is established, and fourteen trees (Physical Mastery + Keeper of the Flame + Tactical Mastery + Battlefield Commander + Vital Operator + Scholar-Warrior + Soldier Athlete + Sovereign Self + The Living Root + Warrior Foundation + Master of the Mind + Cyberspace Operations Officer + Staff Excellence + Cognitive Athlete, 8750 skills) are written and verified as of v165. What's left is repeating that process for the other 2 Mythic trees, both second-gen Phase 7: Cyber Operator, Life Architect.

1. **Confirm the current total** before starting, in case anything has drifted. Don't just grep for a fixed expected number — actually walk all 16 Mythic trees down to their Commons layer with a Node script (see the v157–v165 `FINISHED-FEATURES.md` entries for the exact approach) and see which trees already have 625 Commons vs. which have 0. Total `SEED_SKILLS` should read **11274**. If it's off, or if a tree you expect to be untouched already has Commons (or vice versa), something changed and needs auditing the same way v148-v165 did.
2. **Pick the next Mythic tree** to complete from the 2 remaining: Cyber Operator (`cat:"technical"`), Life Architect (`cat:"personal"`) — re-confirm these cat assignments yourself from `SEED_SKILLS` rather than trusting this list, same standing rule as everything else in this doc. Check for name+cat collision risk in that `cat` first via the duplicate sweep in the Ship checklist. **Both remaining trees have a shipped same-cat sibling**: `technical` (Cyber Operator) has a sibling (Cyberspace Operations Officer) on top of its 5 known dupes; `personal` (Life Architect) has a sibling (Sovereign Self) on top of its 4 known dupes — `personal` has fewer known dupes, worth weighing as a tiebreaker. **The standing v161 finding applies to both with equal force**: grep every candidate name against the whole cat before writing, not just against the 10-item known list — this discipline is no longer optional. **Also check v165's finding**: before writing agent prompts, check whether the chosen tree's OWN groups cluster thematically (as Cognitive Athlete's did) — if so, explicitly name the specific sibling group and scope boundary in each agent prompt, not just a generic "don't duplicate the sibling tree" warning. Each tree is 125 setKeys / 625 skills, same shape as v150–v165.
3. **Reuse the v150–v165 Common object shape exactly** — no redesign needed: `rarity:"common"`, `fadeDays:30`, `setKey` = the parent Uncommon's own `synthesizedFrom` value, 4-item `levels`/`roadmap`/`advance`/`maintain` arrays, no `synthesizedFrom`/`unlockHint`/`tiers` fields. Grep any `phys_c_*`, `hearth_c_*`, `tac_c_*`, `tac2_c_*`, `lead_c_*`, `lead2_c_*`, `phys2_c_*`, `acad_c_*`, `physb_c_*`, `pers_c_*`, `roots_c_*`, `cog_c_*`, `cog2_c_*`, or `tech_c_*` skill for a concrete reference.
4. Write `planning/IMPROVEMENTS-vNNN.md` scoping the chosen tree, then execute with the SLOT-marker + parallel-subagent pattern (see "Writing seeds at scale" below) — one agent per parent Rare (5 setKeys/25 skills per agent). **Cap concurrency at 2 agents per wave, not more** — v154 reduced from 4 to 2 after two file-corruption incidents happened at 4-agent concurrency, and v155–v165 all confirmed zero incidents at 2-agent concurrency across eleven more full trees. Don't scale back up.
5. **In every agent prompt, instruct a strong preference for the Edit tool over any script-based fallback.** The Edit tool has live staleness detection against the real file. A script that reads the file into memory and later writes a full-content replacement — even via a "safe" atomic temp-file-then-rename pattern — can still silently clobber a concurrent agent's edit that landed in between the read and the write (a "lost update"; atomic rename only prevents the file being *observed* mid-write, not this). If a script is truly unavoidable, it must re-read fresh immediately before each individual write and do a minimal targeted replacement, never batching multiple markers into one read-modify-write-all cycle. NEVER use `open(path,'w')`/`fs.writeFileSync(path,...)` directly against the shared target file — that truncates it to 0 bytes the instant it opens, before new content is written.
6. **Explicitly warn every agent about the group-boundary trailing-comma failure mode found in v157** (see above): the last skill object an agent writes, if it's the last in its assigned group, sits right before another group's comment header. It must still end with a comma — the array doesn't care that a comment intervenes. This is easy to miss because the comma requirement depends on content outside the agent's own assigned scope.
7. **Verify after EVERY wave, not just at the end.** After each wave, run ALL of:
   - `node --check`
   - a rarity-agnostic member-count sweep for that wave's setKeys
   - a check that the expected number of `SLOT:` markers actually disappeared (a marker count that doesn't drop by exactly 5×(agents in wave) means someone left stale marker text behind even if their skill content is otherwise correct)
   - `grep "// SLOT:" <file> | sort | uniq -d` — confirm zero duplicate marker lines. v158 found several agents independently hit a transient marker-duplication artifact under heavy concurrent editing; harmless when self-caught (the standard "re-grep after every Edit call" instruction catches it), but a raw marker-count-dropped-by-N check alone won't flag a marker that's merely *duplicated* rather than missing.
   - a full required-field/array-length validator across that wave's new skills: confirm every skill has `name/cat/rarity/fadeDays/setKey/why/howTo/levels/roadmap/advance/maintain`, that `levels`/`roadmap`/`advance`/`maintain` each have exactly 4 items, `rarity==="common"`, `fadeDays===30`, and none of `synthesizedFrom`/`unlockHint`/`tiers` are present. A member-count sweep alone CANNOT catch a skill that exists, counts correctly, but is missing a required field.
   - don't just check that this wave's OWN setKeys are still at 5 members — a lost-update race can silently revert a DIFFERENT, already-verified setKey from an earlier wave. If a member count for a previously-clean setKey unexpectedly drops, that's the signature of this exact race; fix it by hand rather than re-dispatching (redispatching risks racing again). v158 wrote a small reusable Node validator script for this (member-count + field-shape + marker-count + whole-file dup sweep in one script) and re-ran it cumulatively across all previously-completed setKeys periodically, not just the new wave — worth doing the same rather than rewriting the checks from scratch each wave.
   - **also scan UNDISPATCHED groups' markers/headers for corruption, not just the just-completed wave's own content (v159's finding).** A marker split across two lines or a duplicated group-header comment sitting in territory nobody has been dispatched to yet is invisible to any per-wave "did my own markers survive" check — it will just get silently handed to whichever agent is dispatched to that group next, who has no reason to suspect the marker text itself is malformed. v159 caught both of its defects this way: a quick `grep "// SLOT:" <file>` and `grep "GROUP [0-9]* |" <file> | sort | uniq -d` across the WHOLE remaining marker block (not just the wave you just ran) after every wave is cheap insurance.
   - **if the tree's `cat` already has another shipped Mythic tree in it, check name collisions against that WHOLE cat, not just the 10-item known-dupe list (v161's finding).** Wave 1 of v161 introduced 3 collisions against the already-shipped legacy Tactical Mastery tree (same `cat:"tactical"` as the new Warrior Foundation tree) — generic training-terminology names happened to already exist there. The duplicate sweep in step 11 below still catches this at the end, but it's cheaper to have every agent grep `{name:"<candidate>", cat:"<this tree's cat>"` per-name before writing, especially in a cat that already has 625+ shipped skills from a sibling tree.
8. **Budget for a possible mid-dispatch interruption anyway.** Several past sessions hit an account-wide session-limit/stall cutoff partway through a dispatch. If interrupted, a small canary agent (one group) is a cheap way to confirm the limit has lifted before re-dispatching the rest. This includes leftover/inherited content from a prior interrupted session — count members per setKey (and run the required-field validator) on any leftover file before trusting it, the same as newly-generated content.
9. **If the file gets corrupted (truncated to 0 bytes or otherwise unreadable) mid-session:** stop all agents immediately, then check in this order: (a) `git diff`/`git show HEAD:<path>` — but confirm HEAD is actually up to date first, don't assume it is; (b) OneDrive version history (right-click the file in Explorer, or onedrive.com) for a snapshot from just before the incident — verify byte-for-byte via the full required-field+member-count+dup sweep before trusting it; (c) only as a last resort, accept the loss and rebuild from the last good state you can find, redispatching the lost waves.
10. After writing the full tree, run a **recursive whole-tree sweep** (walk Mythic → Legendaries → slot-holders → Uncommons → Commons, confirming exactly 125 setKeys and exactly 625 Common skills, all at 5/5/5/5) AND re-run the full required-field validator across all 625 Commons together — rather than only checking each wave's own setKeys/skills in isolation.
11. Run the full verification: syntax check, name+cat duplicate sweep across the **whole file** (expect exactly 10, the known pre-existing list, zero new).
12. In each wave's agent prompts, explicitly call out the v153 mistake (a full agent omitting `howTo` from all 25 skills), the v154 incidents (truncating writes, lost-update races), and the v157 boundary-comma mistake as concrete failure modes to avoid, and require the agent to self-verify `why`/`howTo` presence and the trailing comma on a sample of its own output before reporting done.
13. **Commit the finished tree's work to git before ending the session.** Don't leave a completed tree sitting only in the working directory.

## Storage discipline — read before adding fields to every skill

v147's storage refactor bought real headroom, but the user's stated goal is 10,000+ skills. Before adding ANY new field that would exist on every seed (and by extension every live skill), ask: **does this need to be duplicated onto the live object, or can it be resolved from the seed via `skSeedOf()`/hydration like the guidance fields now are?**
- Static/authorial content (guidance text, ladder descriptions, tier labels, anything that's the same for every user) → belongs in `SEED_SKILLS` only, resolved live. Do NOT add it as a literal field copied onto `S.lifeSkills` entries.
- User-owned data (progress, history, personal notes, targets) → must stay on the live object; this is the only data that actually needs to scale per-skill in localStorage.
- If you do add a new per-skill static field to `SEED_SKILLS`, check whether `skHydrate()`'s `_SK_GUIDANCE_FIELDS` list (in `skills-core.js`) needs the new field added so it gets the same non-enumerable-getter treatment instead of being persisted.

---

## Required workflow summary

```bash
# After every feature or batch of changes:
python scripts/build.py       # must say OK
npm run check                 # must say SYNTAX OK
npm run regress                # must say PAGEERRORS 0

# After all features, before reporting done:
# bump sw.js: operations-v165 -> operations-v166 (or whatever's next)
# bump SKILL_LADDER_VER in src/core/migration.js only if you changed an EXISTING ladder/tier/guidance text (currently 117) — pure additions don't need it, mergeNewSeedSkills() picks up new seed names on any old save regardless of ladder version
npm run package               # produces dist/operations.zip
```

### Ship checklist (same every session)
0. **At the very start of the session, before any other work:** run `date "+%Y-%m-%d %H:%M %Z"` and note the timestamp (see "Session time logging" in `CLAUDE.md`) — needed for step 14 below.
1. `python scripts/build.py` → `OK index.html`
2. `npm run check` → `SYNTAX OK`
3. `npm run regress` → `PAGEERRORS 0`, check `badCount`
4. Run a duplicate `name`+`cat` sweep across `SEED_SKILLS` (Node script loading `skills-data.js` as a module) — regress won't catch this
5. If you added or claimed to fix any pyramid set membership, run the rarity-agnostic `skSetMembers()`-style member-count sweep too (see "Two auditing traps" above) — regress won't catch a 7-member set either, it only validates individual skill objects
6. Run a full required-field/array-length validator on any new skills — regress validates individual skill objects but won't catch every field-shape mistake a wave might make
7. Bump `sw.js` cache version — **only if something actually shipped**; if a session's work nets out to a revert, leave the version alone
8. Bump `SKILL_LADDER_VER` in `src/core/migration.js` **only if** an existing ladder/tier/guidance changed
9. `npm run package` → produces `dist/operations.zip`
10. If you wrote an `IMPROVEMENTS-vNNN.md` for this session, delete it once implemented (it's recorded in `FINISHED-FEATURES.md`)
11. If incomplete, write `planning/IMPROVEMENTS-vNNN+1.md` for the remainder
12. Add a `planning/FINISHED-FEATURES.md` entry — including an honest one if the session's net result was "investigated, found nothing to do, reverted a false start"
13. Update `planning/NEXT-SESSION-PROMPT.md` (this file) with new version numbers
14. Tell Wyatt to **hard-refresh / reopen the app** so the new service worker activates and any migration runs (skip if nothing shipped)
15. Run `date` again, compute elapsed time since step 0's timestamp, and append a row to `planning/SESSION-TIMES.md` (version/tree worked, start, end, elapsed, and any interruption notes)
16. **Commit the session's work to git** — `src/core/skills-data.js`, `sw.js`, and the planning docs touched. Don't let a finished tree sit uncommitted; v153 and v154 both did this and it directly caused v154's file-corruption incident to require a manual OneDrive recovery instead of a trivial `git checkout`.

### What not to do
- Don't trust an inherited audit/gap-count doc without re-deriving it from `SEED_SKILLS` first — **this includes `NEXT-SESSION-PROMPT.md` itself**, which was found stale in v157.
- Don't count "rarity-filtered members" when checking set completeness — count all members of the `setKey`, matching `skSetMembers()`'s actual behavior. See "Two auditing traps" above.
- Don't read reference docs and then skip reading the actual source files — the code is what matters.
- Don't stop after one cluster and call it done unless you've explicitly scoped the session that way in an `IMPROVEMENTS-vNNN.md` doc.
- Don't add error handling, fallbacks, or validation for scenarios that can't happen.
- Don't add comments explaining what the code does — only the why if it's non-obvious.
- Don't restructure or reformat unrelated code while making a targeted change.
- Don't parallelize structural (Mythic/Legendary/Rare) edits across subagents — too interdependent, do it yourself. Only parallelize bulk seed-writing (Uncommons, Commons), using the SLOT-marker pattern.
- Don't add a new field to every seed/live skill without reading "Storage discipline" above first.
- Don't reuse a `name`+`cat` pair, or a `synthesizedFrom` key, that's already used elsewhere in the file — grep before finalizing, every time.
- Don't trust a wave "succeeded" just because member counts are right — also check that stale `SLOT:` marker comment lines were actually deleted, that every skill has all required fields present, AND that the trailing comma at each group boundary is intact (v157's finding — a missing comma between an agent's last object and the next group's comment header breaks `node --check` for the whole file).
- Don't assume an agent's own self-reported "I verified X" is sufficient — your own independent post-wave validator is what actually catches these things.
- Don't scale concurrency back up past 2 agents/wave — v154 through v165 collectively represent 12 full trees written at 2-agent concurrency with zero file-corruption incidents; 4-agent concurrency caused two in a single v154 wave.
- Don't assume the 10-item known name+cat dupe list is the only collision risk — a new tree sharing a `cat` with an already-shipped sibling Mythic tree can collide against that sibling's own skill names too (v161's finding). Grep per-candidate-name against the whole cat, not just the fixed list.

---

## Key architecture reminders

- `index.html` is **assembled output** — edit `src/`, then build
- All data in `localStorage["operations_v2"]` via `S = load()`; `DEFAULT` is in `src/core/constants.js`
- `skLeafColor(eff, max, sk?)` → `rgb(r,g,b)` string; optional `sk` returns amber if at-risk
- `skEffectiveLevel(sk)` → working level accounting for decay + 20% grace, floors at 1 if started
- `skFadeState(sk)` → `"current" | "at-risk" | "decayed"`
- `skDaysLeft(sk)` → days until actual decay (after grace); null if not started
- `skPractice(skId)` → resets fade timer without level change (non-auto, started skills only)
- `skReachLevel(skId, targetLevel, note?)` → levels up to targetLevel, stores optional note in history
- `skEmblemSvg(sk, eff, max)` — sigil generator in `skills.js`, also used in `trophies.js`
- `miniSparkline(values, w, h)` — small SVG sparkline, defined in `state.js`
- `toast(msg)` — bottom toast, defined in `events.js`
- `PATH_META` — path metadata (name, icon, color, world, lore), in `constants.js`
- `SK_PATH_ICON` — path → emoji map, in `tree.js`
- `renderBosses()` lives in `src/core/state.js` (not a dedicated bosses.js)
- All CSS in `src/styles/main.css` — no per-tab CSS files
- Regression covers **18 tabs** (see `scripts/regress.js`)
- No network calls, no CDN fonts, no telemetry — ever

**Live-skill storage (v147+):**
- `S.lifeSkills` entries hold ONLY user-owned data: `id, name, cat, parent, group, fadeDays, auto, currentLevel, lastQuestTs, peakLevel, targetLevel, history, seeded, joker` (plus pyramid fields `synthesisUnlocked`, `pyramidResetApplied`).
- Guidance text (`why/whatYouDo/howTo/prep/recover/safety/roadmap/advance/maintain/tiers`) and `levels` are **not stored** on seeded live skills — `skHydrate(sk)` (skills-core.js) attaches non-enumerable getters that resolve them from `SEED_SKILLS` via `skSeedOf(name, cat)` at read time. Reading `sk.why` / `sk.levels` works exactly as before; nothing else needs to change when you read these fields.
- `skHydrateAll(list)` is called from `seedSkillsIfEmpty()` (fresh installs) and at the end of `mergeNewSeedSkills()` (every returning-user load) — it also strips legacy literal fields from pre-v147 saves and reports whether it stripped anything, so `mergeNewSeedSkills()` can force a `save()` on the spot.
- `skSeedOf(name, cat)` is now backed by a memoized `Map` (`skSeedIndex()`), O(1) per lookup — safe to call in render loops even at large skill counts. **This is exactly why name+cat collisions matter**: the Map can only hold one seed per key, so a collision means one skill silently becomes unreachable by lookup even though both objects exist in `SEED_SKILLS`.
- Custom (user-created, non-seeded) skills are never hydrated — they keep their own literal `levels` and have no guidance fields (same as before).
- `save()` (state.js) now catches `QuotaExceededError` (or any storage failure) and toasts instead of crashing — but the real fix is not re-introducing per-skill duplication; see "Storage discipline" above.

**Pyramid system:**
- `skRarity(sk)` — rarity from explicit `rarity` field or ladder depth (≤4 Common, 5-7 Uncommon, 8-10 Rare, 11-13 Legendary, 14+ Mythic, auto/joker Joker). **Auto/joker check happens first** — a pre-existing skill with an `auto:` field (e.g. a test-driven skill like "Reaction speed") is a Joker regardless of its ladder length, even if that length would otherwise read as Rare-tier. **A Joker can still be a legitimate, load-bearing member of a Legendary's 5-slot Rare set** — don't exclude Jokers when counting whether a set is complete (see "Two auditing traps" above).
- `skSeedOf(name, cat)` — find a skill's seed in SEED_SKILLS (O(1), see above)
- `skSetMembers(setKey)` — all non-group seeds with matching setKey (rarity-agnostic — includes Jokers). **This is the authoritative definition of "how many members does this set have" — match it exactly when auditing, don't filter by rarity first.**
- `skSetMasteredCount(setKey)` / `skSetCanCombine(setKey)` — set progress. `skSetCanCombine` requires ALL members (Jokers included) individually mastered — this is why a set with more than 5 members is a real regression, not a cosmetic issue.
- `skCombineSet(setKey)` — finds the FIRST seed whose `synthesizedFrom===setKey` and unlocks it. **Never reuse a `synthesizedFrom` key across two different skills** — the second one becomes permanently unreachable (this is the exact bug fixed in v145).
- `SYNERGY_PAIRS` — 15 complementary skill pairs; `skHasSynergy(sk)` — partner at L4+?
- Seeds use: `rarity`, `setKey`, `synthesizedFrom`, `unlockHint` fields
- Live skills use: `synthesisUnlocked` (boolean, the only pyramid field on live data)
- Live skills use: `pyramidResetApplied` (int, set once when skill first gains setKey — prevents re-wipe)
- Side Deck (unstarted leaves): collapsible `<details class="sk-side-deck">` in `skills.js`
- Face-down card function: `faceDownCard(sk, suit, rank, isSynthPending)` in `skills.js`
- Combine button handler: `data-skcombine` in `events.js` delegation
- Chain view: `renderSynthesisChain(cat)` in `skills.js`; toggle `.sc-toggle[data-sctoggle]` wired post-render; output in `.sc-wrap#sc-{cat}`

**Existing skill integration rule (applies whenever wiring existing skills into a new cluster):**
When arriving at a new cluster, first grep existing `SEED_SKILLS` for that `cat` and audit ladder depths. Assign each existing skill that fits by adding `setKey` (and if needed `rarity` or `synthesizedFrom`) directly to its seed object. Rules:
- Existing skills fill slots, not complete sets. At most 2 existing skills per set of 5; never all 5 from existing seeds.
- A skill that covers multiple aspects is OK as one slot — its extra aspects are represented by other new seeds.
- Don't force every existing skill into the pyramid. If one doesn't fit, leave it without a setKey.
- **Sets must have exactly 5 members, full stop — counting every member regardless of rarity/Joker status. If a set already has 5 total members (even if only 3 are "true Rare" and the other 2 are pre-existing Jokers), it is already complete and a new skill cannot be added, structurally or as a fix for a perceived gap.**
- Add explicit `rarity` field when the depth-based auto-rarity is wrong for the skill's role.

**Progress reset rule — intentional, permanent, user-authorized:**
> *"I want to be a blank slate even if I have in the past reached a certain level. I want to go back to basics and earn progress."*

When an existing skill gains a `setKey`, its live progress is wiped (`currentLevel→0`, `history→[]`, `lastQuestTs→null`). It moves from the face-up deck into the Side Deck. `pyramidResetApplied` flag prevents re-wipe on subsequent migrations. Already implemented in `src/core/migration.js` — no changes needed unless adding new pyramid paths.

**Writing seeds at scale (established v145, refined through v165):** pre-place unique `// SLOT:<setKey>:<name>:<cat>` marker comments yourself in one edit, then dispatch one subagent per marker group in parallel, **capped at 5 setKey groups (25 skills) per agent, never more**, and **capped at 2 agents concurrently per wave (not 4)** — v154 hit two real file-corruption incidents at 4-agent concurrency and had zero incidents after dropping to 2; v155–v165 confirmed this across eleven more full trees. Concurrent agents editing the same file will hit Edit-tool staleness retries — that's expected and safe, not a failure; **instruct every agent to strongly prefer retrying the Edit tool over falling back to a script**, since the Edit tool does real staleness detection against the live file. If a script fallback is truly unavoidable, it must NEVER use a truncating write mode (`open(path,'w')`, `fs.writeFileSync(path,...)`) directly against the target — write to a uniquely-named temp file and atomically rename it in. But even atomic rename is **not** sufficient on its own: a script that reads the full file, computes a full replacement in memory, and later writes it via atomic rename can still silently overwrite ("lost update") another agent's edit that landed in the gap between that read and that write. Minimize this by reading fresh immediately before each individual write and never batching multiple markers into one read-modify-write-all cycle. Verify with the full build/check/regress pipeline yourself once at the end, not per-agent — but DO run the syntax + member-count + marker-count + required-field-validator sweep after **every wave**, not just at the end. Always run both the duplicate `name`+`cat` sweep and the rarity-agnostic member-count sweep too, plus a final recursive whole-tree sweep once a tree is fully written. **Also explicitly warn agents about the group-boundary trailing-comma failure mode (found v157)** — the last object an agent writes in its assigned group must still end with a comma if the array continues after an intervening comment for the next group. **If the file is ever found truncated/corrupted mid-session:** stop all agents immediately, don't assume `git HEAD` is current (verify it actually matches expectations first), and check OneDrive version history for a recent snapshot before accepting any data loss — verify any recovered version byte-for-byte with the same full sweep before trusting it.

---

## Tone constraints

Wyatt values: honesty, measurability, privacy, preserved progress, Yggdrasil symbolism. Keep copy plain and honest — no hype, no fake metrics. Ask before large architectural changes. Small surgical diffs.
