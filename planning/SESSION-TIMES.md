# Session time log

Purpose: a ground-truth record of how long real work sessions actually take, so future estimates (e.g. "how long will the remaining Commons trees take") are based on measured data instead of inferring from git commit timestamps — which is unreliable here since multiple versions/workstreams often get squashed into a single commit (see the v144→v151 span, which mixed pyramid structural repair, Phase 7 structure, and two Commons trees into one ~22-hour commit gap that mixes work and non-work time).

**How to log a session (see the "Session time logging" note in `CLAUDE.md`):**
1. At the start of substantive work on a phase/tree/feature, run `date "+%Y-%m-%d %H:%M %Z"` and note the timestamp.
2. At the end of that session (as part of the ship checklist), run `date` again, compute elapsed wall-clock time, and append one row below.
3. Note interruptions (e.g. an account-wide session-limit cutoff) in the Notes column rather than silently folding the recovery time into the total — that's real signal about variance, not noise to hide.

This is a local planning/dev-process log only — wall-clock bookkeeping for estimating future work, not user-facing telemetry, and it never leaves this repo.

| Date | Phase / Version(s) | Start | End | Elapsed | Notes |
|---|---|---|---|---|---|
| 2026-08-17 | v202 (second of 3 confirmed post-audit redesigns: built Signal Intercept, a new stealth-assessment game, for the permanently-stuck Pattern recognition skill; wired Memory retention to real SRS stats) | 2026-08-17 15:33 EDT (inherited from v201's end-time — back-to-back continuation, no gap) | 2026-08-17 16:02 EDT | ≈29m | **Direct continuation of the same conversation as v201**, no idle gap. Confirmed the concrete game mechanic for Pattern recognition via `AskUserQuestion` (sequence prediction, "Signal Intercept" theme) before building — matching the project's own precedent that new game-design surface gets confirmed first, not just bugfixes. Designed the game closely on the existing Land Nav Relay (digit span) template for architectural consistency: a generator-closure pattern (`siMakeGenerator`) makes the flashed sequence and the correct answer share one source of truth, avoiding a whole bug class where the "next" value gets re-derived from the tail of the shown array. Read the skill's own pre-existing seed data (`whatYouDo` copy already described "find what comes next in a sequence... the app explains the rule afterward") before designing — confirmed the mechanic concept was already implied by data written well before this session. Deliberately capped `scoreToLevel` at L7 of 10, matching the established honest-capping precedent (`nback` caps at L8) rather than inventing content for rule types (subtle/positional, abstract matrix puzzles) this game format doesn't actually test. For Memory retention, read the skill's full ladder text first and found it references streaks/time-windows/memory-palace integration this app doesn't track at all — rather than fake those, built an honest two-factor auto-level (mature-card count + SM-2 ease factor, both already computed by the existing SRS engine, no invented formula) capped at L5. Testing surfaced a real-looking "Target crashed" browser error during an aggressive automated play-through; traced it to 44+ orphaned Chromium processes accumulated from this session's extensive Playwright use (confirmed via a clean-environment re-run completing 8 real rounds without issue, and a DOM-node-count probe across 5 rounds showing zero growth) — not a code defect, logged the diagnostic process rather than just asserting "it's fine." Verified the generator/choice logic with 160 automated sanity checks (8 rounds × 20 trials, zero failures) plus 2 full real-timing play-throughs; verified Memory retention's 3 behavioral cases (insufficient data stays silent, small deck levels correctly, large high-quality deck hits the L5 cap exactly, never exceeds it). Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v202`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v201 (fixed the broken Path-development math; found + fixed the real root cause of the v195 "cosmetic NaN" warning as a side effect) | 2026-08-17 15:19 EDT (inherited from v200's end-time — back-to-back continuation, no gap) | 2026-08-17 15:33 EDT | ≈14m | **Direct continuation of the same conversation as v200**, no idle gap. Wyatt confirmed via `AskUserQuestion` 4 design decisions covering all 3 queued redesigns (catProgressFraction as the canonical Path metric; wire real skill progress into Garden's pathXP for all 10 Paths; build a 10th stealth game for Pattern recognition; wire Memory retention to real SRS stats) then said "please do all of these." Started with the #1-ranked finding (Path-development math). Swapped the broken `catRolledLevel()` for the already-correct `catProgressFraction()` in the 2 remaining call sites (Skills-tab deck header, Plan's headline), removed the now-redundant broken half of the Tree's world label, deleted `catRolledLevel()` entirely once confirmed fully dead via a repo-wide grep. Retargeted the Garden's per-skill-levelup pathXP credit from hardcoded `academic` to the leveled skill's own `sk.cat` (2 call sites in `skills-core.js`), so all 10 Paths' idols now genuinely reflect real skill leveling. While behaviorally verifying via a throwaway Playwright script that seeded 40 real physical skills to level 5 (needed to produce a nonzero `catProgressFraction`), **accidentally reproduced the exact v195-flagged "cosmetic NaN" SVG warning** that this same session had already tried and failed to reproduce across 6 sparse-data scenarios earlier — turned out dense data, not sparse, was the actual trigger. Traced it precisely: `skEmblemSvg`'s `_physical()` sigil generator (`skills.js`) had a seed-hash bitmask typo (`s>>2&2` instead of `s>>2&1`) causing an out-of-bounds read on a 2-element position array under specific seed-hash values, producing `cx="NaN"` on one emblem circle. Confirmed via comparison against `_roots()` (a sibling emblem function using the correct mask for the identical pattern) that this was a copy-paste typo, not a design choice. Fixed the one broken occurrence; grepped for the same pattern and found 2 more (`_leadership`, `_hearth`) but confirmed neither has the same crash risk, left both alone rather than over-fixing beyond what was actually broken. Verified everything behaviorally: correct % renders consistently across deck header/Plan/Tree for a real seeded state; `catRolledLevel` throws if called (fully dead); tactical skill leveling now credits `pathXP.tactical` not `pathXP.academic`; zero `circle[cx="NaN"]` elements exist in the DOM after the same seed that previously produced 6 real console warnings. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress -- --shot` → `PAGEERRORS 0`, `total:12524`, `badCount:0`, tree screenshot confirmed clean. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v201`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v200 (2 confirmed live bugs from a fresh 3-agent audit: Board's invalid path key, Battle Buddy Report's wrong field names) | 2026-08-17 14:52 EDT (inherited from v199's end-time — back-to-back continuation, no gap) | 2026-08-17 15:19 EDT | ≈27m | **Direct continuation of the same conversation as v199**, no idle gap. Wyatt asked to fix the flagged v195 cosmetic SVG NaN warning and find another audit-driven redesign candidate comparable to FM. Attempted to reproduce the NaN warning across 6 sparse-data scenarios (empty skills, single AFT entry, single baseline, zero-weight workout, Tree view, all 18 tabs) — could not reproduce it; logged honestly as unresolved rather than guessing at a fix, likely already changed by the volume of render-path edits since v195. Dispatched 3 parallel read-only audit agents (skills/pyramid/tree/garden/trophies; test/quizzes/records/board/awards; daily-loop tabs) with the same rigor bar as the audits that scoped the FM redesign. All 3 returned genuine, ranked findings. Fixed the 2 small, unambiguous, live bugs surfaced along the way immediately rather than bundling them into a larger redesign — the 3 bigger findings (Path-development math fragmentation/breakage, orphaned auto-leveling skills, competing Dawn recommendation systems) are scoped separately for upcoming sessions, all 3 confirmed wanted by Wyatt. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v200`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v199 (Today tab's compact session preview now shows real per-exercise targets, not just names) | 2026-08-17 14:22 EDT (inherited from v198's end-time — back-to-back continuation, no gap) | 2026-08-17 14:52 EDT | ≈30m | **Direct continuation of the same conversation as v198**, no idle gap. Asked "what's next" after v198 shipped; gave a status summary of the broader "make everything use coach" ask and flagged the one remaining minimal surface — the Today tab's compact session preview (`dawnSessionHtml()`), which only ever showed exercise names by original design. Asked Wyatt via `AskUserQuestion` whether to leave it minimal or add real numbers; he chose real numbers. Wired each row to the same `computeTarget(e,{skey,intensity,rich})` call Coach Today/Session-N cards already make, so the compact preview can never drift from the full page's numbers. Restructured `.ds-ex` into a name-left/target-right flex row, reusing the existing gold-bright target color convention from `.rx-ex-tgt`. Verified via a real Playwright screenshot of the card (not just DOM text) — readable at a glance, no wrapping/overflow even with the session's longest exercise/target strings. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v199`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v198 (warm-up/cool-down items now get real phase-appropriate targets instead of wrong strength-set prose) | 2026-08-17 14:12 EDT (inherited from v197's end-time — back-to-back continuation, no gap) | 2026-08-17 14:22 EDT | ≈10m | **Direct continuation of the same conversation as v197**, no idle gap. Wyatt's terse follow-up ("please do warmups") pointed at the one visible gap the v197 fix hadn't touched: warm-up/cool-down exercises were still showing `computeTargetFallback()`'s tier-4 generic text, which is intensity-graded strength-set prose ("3–4 sets, leave 1–2 reps in the tank") — wrong for movement prep where nobody grades effort or does working sets. Added a phase-aware branch gated on the same `isWarmCool` flag v197's tier-3 fix introduced: extracts the real rep/hold spec most `STRETCH_LIBRARY` names already embed in a trailing parenthetical, via a digit-gated regex (so a descriptive aside like "(don't hold stretches cold yet)" on the hardcoded cardio-raise warm-up isn't mistaken for a spec), falling back to a short phase-appropriate qualifier when no spec exists rather than inventing a number. Verified against the real running app: "Leg swings, front-to-back (10/leg)" now reads "10/leg — easy and controlled"; "Doorway chest/shoulder stretch (hold 30s ×2)" now reads "hold 30s ×2 — relaxed, breathe through it". Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v198`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v197 (fixed 2 real bugs stopping Coach Hub's "Today's Orders" from showing real adaptive numbers: an exercise-name mismatch, and a starter-tier bw/gym fallback + warmup/cooldown false-match bug) | not captured — see note | 2026-08-17 14:12 EDT | not measured, real gap incl. idle time | **Direct continuation of the same conversation as v196, across a real gap** (v196 ended 12:14 EDT; `date` was not re-run at the actual start of this chunk, which began when Wyatt sent screenshots of the live Coach Hub after some idle time) — logged honestly as unmeasured rather than computing a naive elapsed span that would mostly reflect idle time, per this file's standing convention. Wyatt reported the "Today's Orders" card showing generic "beginner starting point" numbers for most exercises despite the v196 data restore. Traced the real cause before guessing: `computeTarget()`'s tier-1 adaptive lookup matches by exact exercise-name string, and the workout entries transcribed in v193/v196 used slightly different names than `SESSIONS`' canonical pool (e.g. "Single leg RDL (each leg)" vs. "Single-leg RDL (dumbbells)") — only the deadlift happened to already match. Renamed 16 logged exercise names to their canonical equivalents (left 3 genuinely ambiguous ones alone) and pushed the fix live through TOC's own `POST /api/projects/operations/data` API — the same path the app itself uses — rather than a direct file edit, then verified against the real running app at `http://127.0.0.1:8081`. A follow-up user question ("you need to fix the todays orders section") turned out to describe the same card by its in-app heading; further investigation of that exact card surfaced a second, independent bug: `computeTargetFallback()`'s tier-3 fallback only checked one `BEGINNER_RX` list (gym or bw) based on the day's gym-access flag, so bodyweight-only accessories that appear even on gym days (glute bridge, pike push-ups, hollow-body hold) had no row to match and fell through to vague generic prose with no numbers. Checking both lists fixed that but immediately surfaced a third bug: `cgFindRxRow`'s word-overlap matcher started false-matching warm-up/cool-down stretches against unrelated strength rows on common short words (e.g. "hold"), producing nonsense text on stretches. Fixed by gating tier-3 entirely on the exercise's `_phase` not being warmup/cooldown/flex. Verified via throwaway Playwright scripts (deleted after use) against the real live app: all 7 Session 1, all 6 Session 4, and 6/7 Session 3 gym exercises resolve to real adaptive/starter numbers; spot-checked several against raw logged sets to rule out cross-exercise data bleed; confirmed warm-up/cooldown items across bw/gym modes for Sessions 1/3/4 are back to correct generic prose with zero false matches. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v197`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v196 (real fix for the TOC save-data-loss bug, then a 3rd data restore backed by the fix) | 2026-08-17 11:18 EDT (inherited from v195's end-time — back-to-back continuation, no gap) | 2026-08-17 12:14 EDT | ≈56m | **Direct continuation of the same conversation as v195**, no idle gap. Wyatt reported `personal/toc-save.json` had reverted to empty again — the same real data restored during the v193 session (and re-restored once more mid-that-session after a first reversion) was gone a third time. Rather than repeat the same unprotected restore-and-hope pattern, traced the actual root cause in `tocInit()`/`tocWriteDebounced()` (`app-setup.js`) before touching the data: `_tocPresent` (gating whether writes are allowed) was set true as soon as TOC's health-check answered, *before* the separate fetch for the real saved data resolved — on a slow/cold local backend response exceeding the 800ms timeout, the code fell into a catch block that left `_tocPresent` true anyway ("keep syncing writes"), so the very next `save()` for any reason (even an automatic `checkDailyReset()` inside a normal render cycle) would silently flush the still-fresh/default local state over the real one via the debounced `tocFlush()`. Fixed by adding a second flag, `_tocDataConfirmed`, distinct from `_tocPresent` — writes now require both, and confirmation only comes from a definitive fetch outcome (data adopted, or an explicit empty response), never a timeout; the data fetch also now retries once with a more generous 3000ms timeout before giving up, since a single missed 800ms window was the plausible actual trigger. Verified behaviorally, not just by reading the fix: a throwaway Playwright script (deleted after use) mocked a healthy TOC with a data endpoint that hangs past both retry windows, confirmed `_tocPresent:true, _tocDataConfirmed:false`, and confirmed calling `save()` twice afterward triggered zero write attempts — exactly the scenario that caused the real data loss. A second check confirmed the ordinary fast-success path still works and correctly sets `_tocDataConfirmed:true`. A live check against the actual running TOC backend on this machine (not mocked) confirmed the safety property holds for real, even though the `file://`-origin test environment itself couldn't complete the data fetch (a known testing-environment restriction — the real user path via `http://127.0.0.1:8081` was already confirmed working during the v193 session). Then restored the real save data a third time — migrated fresh from `personal/save.json` through the app's real migration pipeline plus the 6 workout sessions added earlier this conversation — written in an isolated, TOC-blocked Playwright pass, this time backed by the actual fix rather than a repeat of the same unprotected path. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v196`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-17 | v195 (ground-up FM redesign: Coach Hub, unified 4-tier computeTarget(), AFT-scaled starter numbers) | not captured — see note | 2026-08-17 11:18 EDT | not measured, real gap | **A real day-boundary gap occurred before this session** (v194 ended 2026-08-16 18:13 EDT; the system's own date changed to 2026-08-17 partway through this conversation), unlike the back-to-back v192→v193→v194 continuations — `date` was not re-run at the actual start of this chunk. Logged honestly as unmeasured rather than computing a naive elapsed span that would mostly reflect idle time, not real work — same convention as the v190 entry's day-boundary gap. Real work covered regardless: Wyatt asked for a ground-up redesign of the FM tab and connecting tabs "to be better and more properly suited to the redesign," plus the broader AFT-informed-starter-numbers feature. Given the scope, ran the project's full plan-mode process rather than guessing: a general-purpose audit agent read Plan/Log/AFT/card-game end to end and produced a structured pain-point report (found 3 competing "what should I do" systems on the same page, `computeTarget()` with no home screen, several flat-out duplicated small systems); a second Explore agent verified new-tab mechanics / AFT scoring bands / the design-token system; a Plan agent turned both into a concrete file-by-file plan; Wyatt confirmed 3 direction decisions via `AskUserQuestion` (unified Coach Hub restructuring Plan in place rather than a new tab; keep the 3 tabs' distinct visual identities rather than unify them; a plain AFT-fitness multiplier on generic starter weights rather than a per-exercise formula) before any code was written — the plan was written to a plan file and approved before implementation began. Built in two phases per the plan: Phase A (logic — extended `computeTarget()` to 4 tiers with tiers 3/4 strictly opt-in via an `opts` param to protect existing bare-call callers, added `aftFitnessMultiplier()` reusing the existing 300/350 sparkline-coloring bands, collapsed `prescriptionFor()` into a thin wrapper, unified `cgSlotVolume()`/`renderSessionLists()`'s fallback onto the one engine, relocated Monthly Baseline's logic from `plan.js` to `log.js` after grepping for external references, deleted AFT's static `DRILL` advice + the duplicate sparkline, added 2 mock-AFT buttons to the AFT tab, relabeled (not deleted) the Log form's 3 rating scales, added 2 new cross-tab links) verified independently before Phase B (plan.html restructured into a `#coachHub` + "Schedule & Setup" split, the 3 per-tab CSS themes extended to cover `#cardGameModal`/`#mockAftModal`/equipment-profile/gym-access cards that had sat outside all three as generic chrome). Caught one real bug during verification, not before shipping: tier 3's target string originally led with the sets count, but `cgSlotVolume()` parses the leading number as reps (matching every other tier's convention) — would have silently handed card-game the wrong rep count; fixed by reordering the string and adding a structured `reps` field. Also found and removed 2 small pieces of newly-dead CSS (`.aft-drill`, `.aft-trend-wrap`/`.aft-trend-range`) left behind by the `DRILL`/duplicate-sparkline deletions. Verified extensively via a throwaway Playwright script: all 4 `computeTarget()` tiers, the AFT-multiplier at all 3 bands plus the no-AFT no-regression case, bare-call backward compatibility, cross-call-site numeric agreement (Coach Today / Session N card / card-game all agreeing on the same exercise), the Coach Hub's DOM structure, the relocated baseline card, only one AFT sparkline existing, and both new buttons/links working. Found (but did not chase down, confirmed pre-existing and unrelated via isolation testing) a cosmetic `<circle>` NaN SVG warning under sparse synthetic data shapes — never threw, `pageerrors` stayed 0 throughout, `npm run regress`'s own data flow doesn't hit it — flagged in the `FINISHED-FEATURES.md` entry for a future look. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` (incl. `--shot`) → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v195`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-16 | v194 (Plan-tab restructure: adaptive coaching merged into the workout view, standalone Adaptive Targets card removed, today's-session auto-open) | 2026-08-16 17:50 EDT (inherited from v193's end-time — back-to-back continuation, no gap) | 2026-08-16 18:13 EDT | ≈23m | **Direct continuation of the same conversation as v193**, no idle gap. Wyatt pushed further right after v193 shipped: "fix all of those buried and hard to find things... also the coaching/recommendations should replace the previous FM stuff where it actually gives the things that are required when looking at the workout instead of needing to find it separately also, the workouts layout should be based on the adaptive coachings scheduling." Read `renderCoachToday()`/`prescriptionFor()` first rather than assuming — found Coach Today's per-exercise line already called `computeTarget()` (wired since v181), so that surface wasn't actually the gap. The real redundancy was `renderSessionLists()`'s Session N reference cards (a separate, non-adaptive `BEGINNER_RX` static table) and the standalone Adaptive Targets card v193 had just relocated — three parallel "what should I do" surfaces, only one truly adaptive. Fixed by merging, not duplicating: `renderSessionLists()`'s rx-card now prefers real `computeTarget()` output over the static beginner row whenever log/AFT history exists; the standalone card (`renderAdaptiveTargets()`, its `#adaptiveTargets` mount, its `state.js` dispatch call) was removed entirely since its content was never unique, just re-listed elsewhere; and a new `openTodaysSessionBlock()` expands whichever Session block matches `todaysPlan().sessionKey` (the same adaptive scheduler already driving Coach Today) instead of Session 1 being hardcoded open regardless of what's actually scheduled — called once on nav-to-Plan, not every render, so it doesn't fight a manual expand/collapse. Removed only the now-genuinely-dead `.adapt-row`/`.adapt-ex`/`.adapt-tgt`/`.adapt-empty` CSS, keeping `.adapt-card`/`.adapt-sub` since `board.js`'s unrelated Cyber-branch fact card reuses that base styling — checked before deleting, not assumed. Verified via a throwaway Playwright script (deleted after use): confirmed `#adaptiveTargets` no longer exists anywhere in the DOM; confirmed `openTodaysSessionBlock()` opens only the correct session and closes the rest (tested by temporarily stubbing `todaysPlan()`, since real "today" in the sandbox is a Sunday/rest day where nothing should be open); confirmed the merged rx-card rows render real, sensible adaptive text sourced from the same engine as Coach Today. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v194`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-16 | v193 (docs cleanup follow-through + adaptive-trainer visibility fix + AFT-history integration) | 2026-08-16 17:19 EDT (inherited from v192's end-time — back-to-back continuation, no gap) | 2026-08-16 17:50 EDT | ≈31m | **Direct continuation of the same conversation as v192**, no idle gap — Wyatt asked to finish the docs cleanup ("get rid of planning docs that no longer have a use and update all docs or delete the non-necessary ones"), then mid-turn raised two real product issues: the adaptive FM trainer being hard to find, and past AFT scores not feeding it. Read `IDEAS-gui-revamp.md`/`IDEAS-tests-fm-workouts.md` in full to confirm zero open items before deleting (both fully shipped, content preserved in `FINISHED-FEATURES.md`), and read `docs/operations-expansion-outline.md` in full to confirm every one of its ~15 proposed features had since shipped before deleting it too — didn't delete on the strength of "looks old" alone. Rewrote `docs/OPERATIONS-HANDOFF.md` (was describing v89/16-tab/93-skill app state) as a short orientation doc scoped not to duplicate `CLAUDE.md`/`FINISHED-FEATURES.md`. Fixed every dangling reference to the deleted docs, including code comments in `cardgame.js`/`test.js` citing now-nonexistent section numbers. For the trainer complaint: traced `renderAdaptiveTargets()`'s "Your Next-Session Targets" card to a collapsed-by-default `<details>` in `plan.html` (a real regression from v190's Plan restructure, which lumped an actionable coaching output in with static settings) — moved it to sit always-visible below Coach Today. For the AFT-history gap: read `computeTarget()` end to end and confirmed it only ever read `S.workouts`/`S.baselines`, never `S.aft` — added AFT blending modeled directly on the existing baseline-test blending mechanism (same corroborating-override pattern, not a new one), plus a seed-from-AFT path for exercises with real AFT history but zero workout-log entries, which previously went silent. Deliberately excluded SDC from the AFT-exercise mapping (no clean 1:1 logged-exercise match exists) and avoided inventing a percentage-of-max formula for the deadlift seed case, consistent with the app's standing "plain rule, not a fitted model" precedent. Verified via a throwaway Playwright script: card visibility (not nested in any closed `<details>`), all 4 seed-from-AFT cases producing sensible output, the SDC exclusion correctly returning null, the corroborating-override case firing correctly on synthetic data, and a no-AFT-data case behaving byte-for-byte unchanged from before (pure regression check). Also spot-checked X-SmartFocus, Insights, Upcoming timeline, the Test tab's focus picker, and nav reachability for the same "buried in a closed collapsible" failure mode — none found; the Upcoming timeline's initial "not showing" reading was its own correct silent-on-empty-data behavior, confirmed by re-testing with real seeded dates, not a bug. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v193`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-16 | v192 (full project cleanup: dead code, real bugs found along the way, planning-doc maintenance) | not captured — see note | 2026-08-16 17:19 EDT | not measured | **Continuation of the same conversation as v191, across a context-window compaction** — no `date` was re-run at the actual start of this cleanup chunk (the conversation summary that survived compaction didn't carry a timestamp either), so elapsed time isn't computable, and the raw wall-clock gap since v191's 11:34 EDT end would mostly reflect idle time between turns, not real work — logged honestly as unmeasured rather than guessed, per this file's own standing convention (see v188/v190 for the same situation). Wyatt declined the queued skills-expansion doc and asked for a full cleanup pass instead: code audit, planning-doc cleanup, further FM/training improvements if any. Found and fixed 6 real bugs while auditing (not just cosmetic dead-code removal) — a `tree.js` window-listener leak, a name-matching bug in FM-3's `cgFindRxRow()` that silently doubled prescribed volume on a mismatch, the Plan tab rx-card showing the wrong exercise on a swap day, no recovery-day spacing in `assignWeekSessions()` (fixed with a bidirectional multi-pass de-stacking swap, verified across 5 gym-access patterns), Intel Briefing having zero real CSS since it shipped in v187 (plus a class-name collision found along the way), and a dead `.sk-pyr-mythic.sk-pyr-maxed` CSS selector. Also removed ~30 orphaned CSS blocks and 3 dead functions (verified each via grep, catching one false positive — `trophies.js`'s dynamically-constructed `.trophy-chip` classes — before deleting), removed 2 inert equipment tags, and added an equipment-requirement warning to the guided mock-AFT flow plus a scope-clarity note to the card-game equipment step. Reordered `planning/SESSION-TIMES.md` (this file) back to its own stated "most recent first" convention after v163–v167/v172 had drifted out of sequence; condensed `planning/FINISHED-FEATURES.md`'s 389-line v149–v167 Commons-layer span into one entry, added a version index, and fixed two stale sections (color palette, Architecture Summary's file list); trimmed `planning/NEXT-SESSION-PROMPT.md`'s closed-workstream re-narration to short pointers and noted the skills-expansion decline. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0` (unchanged — no skills added/removed). Behaviorally re-verified the fixes not covered by regress via throwaway Playwright scripts (deleted after use): `cgFindRxRow`'s word-overlap matching, the Plan rx-card, and the mock-AFT equipment-warning logic (fires for a gear-lacking profile, silent for a fully-equipped one). No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. SW bumped to `operations-v192`. `npm run package` produced `dist/operations.zip`. |
| 2026-08-16 | v191 (GUI-revamp Phase B: remaining-tab token migration, Weight tab identity confirmed, collapsible nav categories) | 2026-08-16 10:48 EDT (inherited from v190's end-time — back-to-back continuation, no gap) | 2026-08-16 11:34 EDT | ≈46m | **Direct continuation of the same conversation as v190**, no idle gap this time (unlike the v189→v190 boundary) — flowed straight from the v190 wrap-up into "what's next" → a 3-option scoping question (all three picked) → two more targeted decisions (Weight tab identity, nav interaction model) via `AskUserQuestion`, then straight into implementation. Migrated 25 remaining `*-card` classes and 12 remaining button classes onto the Phase A token scale via an exact-string-match Python script (avoided a blanket regex sweep so every change stayed individually traceable and verifiable); deliberately excluded `.sk-card`/`.cg-card` (distinct components) and the Weight-tab-exclusive `.wm-btn`/`.wm-nudge-btn`/`.wb-btn`. Documented the Weight tab's confirmed-intentional serif/tan identity with a comment rather than leaving the decision only in conversation history. Built `initNavGroupToggles()` for real click-to-collapse nav category interaction, confirmed safe against the existing click-delegation before writing it (same check as v190: filtered by `[data-tab]`, headers are `data-tab`-less divs). Verified with `npm run regress -- --shot`, a throwaway Playwright script directly exercising collapse/re-expand and cross-group click isolation, and real screenshots of the Test and Weight tabs (confirming the retokenized cards render cleanly and Weight's distinct identity survived untouched). Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. `npm run package` produced `dist/operations.zip`. **This closes out the entire GUI-revamp workstream (open since v167) — no open items remain in `planning/IDEAS-gui-revamp.md`.** |
| 2026-08-16 | v190 (FM subsystem audit + font self-hosting + GUI-revamp Phase A: design-system foundation, Plan/Log/AFT restructure, nav reorg) | not captured — see note | 2026-08-16 10:48 EDT | not measured, real gap | **A real day-boundary gap occurred before this chunk of work** (the v189 session ended 2026-08-15 18:30 EDT; the system's own date changed to 2026-08-16 partway through this conversation) — unlike v189's back-to-back continuation of v188, this was not a same-sitting continuation, and `date` was not re-run at the actual start of this work (should have been, per the standing instruction — noted again for future sessions). Logged honestly as unmeasured rather than computing a naive 16+-hour "elapsed" that would mostly reflect idle time between conversation turns, not real work. Session covered real ground regardless: audited FM-1/FM-2/FM-Adapt/FM-3 with live behavioral Playwright tests (all four confirmed working correctly, no fixes needed); self-hosted the two Google Fonts (a real pre-existing CLAUDE.md hard-rule violation) after confirming outbound network access worked in-sandbox, fetching the actual variable-font woff2 files rather than guessing at URLs; then a large GUI-revamp pass — two parallel research-agent audits (CSS visual system, nav git-archaeology) plus a Plan-agent design-review, all read and cross-checked directly (not trusted blind — spot-verified several load-bearing claims like the rgba-vs-hex distinction and the exLi how-to gap by reading the actual source) before entering plan mode and getting the phased approach explicitly approved before writing any code. Shipped Phase A only, by design — deliberately did not attempt all 45+ button classes / 40+ card classes / 18 tabs in one pass, given the real regression risk of an unreviewable mega-diff. Verified with `npm run regress -- --shot`, a manual multi-tab screenshot spot-check (traced one apparent nav-duplication artifact to a known Playwright full-page-screenshot quirk with `position:sticky`, confirmed not a real bug by checking the untouched CSS), and a second throwaway Playwright script directly exercising the new nav groups, the Setup section, the exercise how-to expansion, and the Log tab's deduped rating scales. Zero page errors throughout. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. `npm run package` produced `dist/operations.zip` (verified the vendored fonts survived packaging at the correct path via Python's `zipfile` module, after fixing `scripts/package.js`'s `-j` flag which would have silently broken that). |
| 2026-08-15 | v189 (Phase X-SmartFocus: whole-tree leverage recommender — FM/test-features doc now fully complete) | 2026-08-15 18:13 EDT (inherited from v188's end-time, not freshly measured — see note) | 2026-08-15 18:30 EDT | ≈17m | **Continuation of the same conversation as the v188 session, no real idle gap this time** (unlike v187→v188) — the start timestamp here is v188's own end-time carried forward as a reasonable proxy, since work flowed directly from that session's wrap-up into "what's next" → a 4-question `AskUserQuestion` design pass for X-SmartFocus → "please build," with no gap to lose track of. Still logged as an inherited proxy rather than a fresh `date` call, per the same honesty standard as v188's entry — future sessions should still re-run `date` at the actual start of each distinct work chunk rather than relying on this shortcut. Design pass resolved the doc's own flagged blocker ("commissioning readiness" undefined against the tree's 10 Paths) via 4 questions (Path weighting, leverage math, deadline anchor, UI surface); all 4 landed on the recommended option. Implementation was fast once the design was settled: `computeSmartFocus()` (`skills-core.js`) + a callout render (`today.js`) + CSS, no new build-file registration needed (both touched files were already in the JS_FILES lists). Verified via a throwaway real-browser Playwright script: confirmed silence on a fresh save, hand-derived the expected score for two synthetic skills *before* running the check and confirmed the actual computed values matched exactly, and confirmed the callout renders live with correct content. `npm run regress` hit the documented environmental memory-pressure flakiness (a `page.goto` timeout, ~6GB free of 31.6GB) — passed clean on an immediate retry. Full ship checklist passed: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. `npm run package` produced `dist/operations.zip`. **This closes out the entire `planning/IDEAS-tests-fm-workouts.md` build order — no open items remain in that doc.** |
| 2026-08-15 | v188 (focus-picker menu — Phase T's last loose end — plus Phase FM-3: card-game workouts, full session build) | not captured — see note | 2026-08-15 18:13 EDT | not measured | **Continuation of the same conversation as the v187 session, resumed after a real gap (the v187 commit was 2026-08-14, this work landed 2026-08-15) — `date` was not re-run at the actual start of this chunk of work, so start-to-end elapsed time here would be misleading if computed naively (it would include the idle gap between conversation turns, not real work time). Logged honestly as unmeasured rather than guessed; future sessions should re-run `date` at the start of every distinct chunk of work, not just once per conversation.** Wyatt asked "what is next," was given three options (the focus-picker menu, FM-3, X-SmartFocus); FM-3 was flagged as needing an explicit re-greenlight per its own standing doc note, asked via `AskUserQuestion`, and Wyatt confirmed both "yes, build FM-3" and "full session build" (not the one-slot-prototype fallback). Built the focus-picker menu first (small, self-contained). For FM-3, dispatched a research agent to map the *real* FM-2/FM-Adapt data model (`SESSIONS`, `BEGINNER_RX`, `computeTarget`, `equipProfiles`) before writing any game logic, then read the source directly to verify the agent's most load-bearing claims (slot pool sizes, `BEGINNER_RX` name-alignment) before trusting them — found the design doc's "exactly 4 variants per slot with distinct difficulty tiers" assumption doesn't match the real data (1-5 real, undifferentiated exercises per slot), and made a documented, honest simplification (uniform value multiplier) rather than inventing a fake difficulty score. New `src/tabs/cardgame.js`; `npm run regress` doesn't exercise game interaction, so wrote a throwaway real-browser Playwright script that force-started a real `s1` session and played all 6 exercise groups end-to-end (57 draws, deliberately alternating too-easy/too-hard ratings to exercise both adaptive-bias branches), confirming a correctly-shaped `S.workouts` entry; also ran a 200-trial synthetic sweep of the guardrail formula across varied thresholds, confirming draws stay within (or very close to, on small legitimate thresholds where integer rounding loosens the band slightly) the intended 8-12%-of-threshold guardrail. Zero page errors throughout, no bugs found needing a fix this time (unlike v187's Land Nav Relay bugs). Full ship checklist passed clean: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, `SKILL_LADDER_VER` stayed at 118. `npm run package` produced `dist/operations.zip`. |
| 2026-08-14 | v187 (Phase T, sub-phases 2-9: the remaining 8 stealth-assessment constructs, Phase T now fully done) | 2026-08-14 07:30 EDT | 2026-08-14 07:56 EDT | ≈26m | **Directly measured, single-purpose session, fresh conversation resumed from `NEXT-SESSION-PROMPT.md`.** Wyatt asked for all 8 remaining Phase T constructs in one session rather than one-at-a-time as prior sessions had done. Read the full §1a design doc and the v186 Sentry implementation first to lock in the established pattern (reskin only, same underlying `scoreToLevel`/`recordTest()` measurement, hide-during-play/reveal-after rule, render()-then-re-query fix), then built all 8 reskins directly (no subagent dispatch — unlike the pyramid Commons work, this was fast enough and interdependent enough via shared files that solo sequential edits were faster than fan-out/merge overhead). `npm run regress` only clicks tab nav, not individual game logic, so wrote a throwaway real-browser Playwright script (deleted after use) to actually play through every construct — caught two real bugs in Land Nav Relay (a repeat-waypoint tap bug where digit sequences with repeats produced an unwinnable state, and a click-event-bubbling bug that silently skipped a round), both fixed and re-verified. Full ship checklist passed clean: `npm run build` → OK, `npm run check` → SYNTAX OK, `npm run regress` → `PAGEERRORS 0`, `total:12524`, `badCount:0`. No `SEED_SKILLS`/ladder content touched, so `SKILL_LADDER_VER` stayed at 118. `npm run package` produced `dist/operations.zip`. |
| 2026-08-14 | v186 (Phase T, sub-phase 1 of N — Sentry, the reaction-time stealth-assessment game) | 2026-08-14 06:55 EDT | 2026-08-14 07:19 EDT | ≈24m | New session, "please begin the next thing" → Phase T per the confirmed order, starting with Sentry per the doc's own explicit recommendation ("simplest real-game wrapper"). Researched the existing reaction-time drill and game-feel infrastructure first (no canvas/audio anywhere in the app, Go/No-Go's setTimeout-driven stimulus-and-judge pattern is the closest structural template) before building — confirmed the design from §1a was already fully locked in, nothing to redesign. Built Sentry (night-watch theme, 5 threat silhouettes measured for real reaction latency + 3 decoys that punish an over-eager tap) as a drop-in replacement for `startReaction()`, keeping the exact same `scoreToLevel` curve and `recordTest()` pipeline so the underlying measurement is unchanged — only the presentation and the stealth-during-play rule (no ms/timer shown mid-game, only game-native tallies) are new. **Found and fixed a real, previously-unnoticed bug while verifying**: every one of the 8 cognitive tests' results screens was being wiped in the same synchronous tick it was written, because each `*Done()` function wrote its results HTML then immediately called the global `render()`, which unconditionally regenerates the whole test list (including a blank stage div) as a side effect — confirmed directly (not assumed) via a targeted DOM check showing the stage node gets swapped out and emptied. Fixed all 8 by reordering to call `render()` first, then re-query and write results into the fresh post-render element — the same fix, applied consistently, everywhere the pattern appeared. Verified via a full real-browser playthrough of Sentry (drove all 8 rounds programmatically, confirmed the results screen now actually persists with correct threat-latency averages, false-alarm count, a PR flag, and the level-up firing) plus a partial confirmation on Go/No-Go (ran out of the test's own time budget before finishing 25 rounds, but the identical code pattern was already conclusively proven on Sentry). Also bumped `SKILL_LADDER_VER` for the one guidance-text edit (Reaction speed's `whatYouDo`, updated to describe Sentry instead of the retired tap-on-green mechanic). Full ship checklist passed clean, including `--shot`. |
| 2026-08-13 | v185 (Phase X-Insight — cross-domain pattern surfacing) | 2026-08-13 08:41 EDT | 2026-08-13 19:28 EDT | ≈10h47m (see note) | **Not continuous working time** — the elapsed span includes real-world gaps; the actual design/build/verification work (data-shape verification, 3-check design, `insights.js`, `records.js` wiring, both build-script updates, CSS, and verification) was a single focused pass, but system memory pressure repeatedly stalled Playwright browser launches during verification (diagnosed via `Get-CimInstance Win32_OperatingSystem`: ~4GB/31.6GB free, consistent with the same environmental issue documented during v175's session), which is most of why the wall-clock gap is large rather than the work itself taking that long. Read the actual data shapes directly (`S.streakLog`/`S.aft`/`S.weightLog` date formats, confirmed via grep, not trusted secondhand from the earlier research agent) before designing. Built 3 grounded cross-domain checks — streak consistency vs. AFT score, training frequency vs. AFT improvement, body-weight movement vs. deadlift movement — each deliberately silent unless both sides of a comparison have real support (≥2 per bucket, ≥4 total), and phrased as plain bucketed-average/co-occurrence comparisons rather than a correlation coefficient (avoiding false statistical precision on a small personal dataset). When direct browser verification kept stalling, fell back to a memory-light Node `vm` sandbox to verify the pure logic (empty state silent, one-sided data silent, a rich synthetic dataset correctly triggering all three checks with honest N-counts in the phrasing) — confirmed genuinely equivalent coverage to a browser test since the module has zero DOM dependencies, then did one lighter real-browser pass at the end to confirm the actual UI card renders. `npm run regress` needed one retry (succeeded second time) for the same memory-pressure reason. Ship checklist passed clean. |
| 2026-08-13 | v184 (standing instruction: every subjective self-rating scale in the app uses 1-10 — audited and converted the remaining two, session RPE and PT intensity) | 2026-08-13 08:10 EDT | 2026-08-13 08:22 EDT | ≈12m | Wyatt generalized v182/v183's pattern: "for any effort level or self rating thing or anything else similar to this please have it use a 1-10 scale" — a standing instruction, not a one-off. Rather than guess at scope, dispatched an Explore agent to audit every `&lt;select&gt;`/emoji-button rating input across all of `src/`. Found exactly two more genuine matches — `#ptIntensity` (PT session "how hard," was light/moderate/hard) and `#lgRpe` (workout RPE, was a `&lt;select&gt;` oddly capped to 6-10 only, missing 1-5) — and confirmed everything else (task-difficulty XP tiers, award/membership categories, the ILR language-proficiency standard scale, SRS/Anki-style spaced-repetition grading) is objective categorization or a load-bearing algorithm input that genuinely should NOT become 1-10, with reasoning for each. Converted both matches to the same 10-button pattern as the effort/readiness scales. Caught a real calculation dependency before shipping: `recoveryLoad()` (feeds `plan.js`'s PT-fatigue-aware session easing) used `{light:1,moderate:2,hard:3}` as a decay weight — converting the input to 1-10 without adjusting this would have silently thrown off the `renderRecoveryAdvisory()` fatigue/soreness thresholds calibrated against that old 0-3 range, so the new numeric intensity is scaled `(intensity/10)*3` to land in the same effective range, with a fallback for legacy string values already in `S.ptLog`. Verified via direct calls and real UI clicks (PT intensity default-highlight and click-to-set confirmed live; RPE scale confirmed via full UI click-through and a saved-workout round-trip; `recoveryLoad()`'s output confirmed at the expected scaled magnitude for a rated-9 PT session). Ship checklist passed clean. |
| 2026-08-13 | v183 (quick fix: pre-session readiness check-in changed from 3-way rough/normal/strong to 1-10, matching v182's effort scale) | 2026-08-13 08:04 EDT | 2026-08-13 08:10 EDT | ≈6m | Direct follow-up to v182 — Wyatt asked to make the readiness check-in 1-10 too, for consistency with the effort rating. `LG.readiness`/`S.workouts[].readiness` changed from a string enum to a number; log form renders 10 numbered buttons instead of 3 emoji buttons. Readiness still isn't fed into `computeTarget()`'s math (unchanged design decision from v181 — it's pre-session, can't retroactively adjust an already-computed target). Verified via real UI clicks: 10 buttons render, click sets/toggles the numeric value, and a saved workout correctly persists it — caught and fixed a test-script ordering bug of my own (changing the session dropdown resets the whole log draft, always has, so the test needed to set the session before rating anything) rather than a real app bug. Ship checklist passed clean. |
| 2026-08-13 | v182 (quick fix: per-exercise effort rating changed from 3-way easy/right/hard to a real 1-10 RPE scale) | 2026-08-13 07:57 EDT | 2026-08-13 08:04 EDT | ≈7m | Wyatt asked directly to change the FM-Adapt effort scale from 1-3 to 1-10, mid-way through kicking off X-Insight research (that Explore agent's findings arrived after this fix started and are still pending review). Renamed `ex.difficulty` (string enum) to `ex.effort` (1-10 number) throughout `log.js`; the log form now shows 10 small numbered buttons instead of 3 emoji buttons; `computeTarget()`'s hard/easy bands became effort≥8 / effort≤3 (RPE convention) instead of string equality, with a fallback to the existing session-level RPE wording when only that's present. Verified the exact same three logic cases as v181 (hard/easy/neutral bands) now driven by numeric thresholds, plus real UI clicks confirming all 10 buttons render and set the value. Ship checklist passed clean. |
| 2026-08-13 | v181 (Phase FM-Adapt — difficulty-rating-aware adaptive rep/set/weight targets) | 2026-08-13 07:39 EDT | 2026-08-13 07:51 EDT | ≈12m | New session, picking up "please begin the next thing to be done" with FM-Adapt (flagged as needing a design pass first) next in the confirmed order. Before designing anything, read the actual codebase and discovered `computeTarget()`/`renderAdaptiveTargets()` (`log.js`) already existed — a real, working, trend-aware/stall-detecting/baseline-blended next-session-target engine, just never fed by the difficulty/RPE signal it was already half-capturing (`lgRpe` was saved but never read back). This reframed the whole phase: not a new system to design from scratch (the "needs a dedicated design pass" caveat was written before this was known), but a bounded, low-risk extension of trusted existing logic. Added per-exercise "how did it feel" (easy/right/hard) + "had to cut it short" capture to the log form, plus a workout-level pre-session readiness check-in (logged for the record, not fed into the math — kept honest rather than pretending a pre-workout mood tap recalculates anything). Wired the two real signals into `computeTarget()` as a plain rule, not a fitted model: hard-rated or cut-short forces a hold regardless of raw-number trend (safety first); easy-rated nudges a flat trend up a notch; no rating at all leaves behavior byte-for-byte unchanged (verified directly). Also retired FM-2's simpler `lastLoggedWeight()` mechanism in `prescriptionFor()` in favor of calling the same `computeTarget()` engine, so there's one adaptive-suggestion system instead of two quietly-inconsistent ones. Verified via direct function calls (first-entry-hard forces hold, flat-plus-easy pushes, improving-but-reduced still holds, no-rating is unchanged) and real UI interaction (readiness/difficulty/reduced buttons clicked, a workout saved, confirmed the persisted record). Full ship checklist passed clean. |
| 2026-08-12 | v180 (unplanned mid-workstream request: true dynamic warm-up / static cool-down stretches, muscle-matched per session, doubling as Session 5's flexibility pool) | 2026-08-12 20:11 EDT | 2026-08-12 20:31 EDT | ≈20m | **Continuous with the v179 row above.** Not part of the confirmed FM-doc build order — Wyatt asked directly, mid-session, to fix a real correctness gap: every session's "warm-up" and "cool-down" were the same few hand-duplicated static stretches (and Session 2/Run had no cool-down at all), which also quietly violated the app's own stated rule elsewhere ("never static-stretch cold muscles") since the identical stretch lines opened AND closed some sessions. Built a proper fix rather than a relabel: new `STRETCH_LIBRARY` (12 dynamic + 15 static entries, each muscle- and equipment-tagged) plus `AREA_MUSCLES` mapping each session's coarse `areas` to real muscle groups; `warmupStretchesFor()`/`cooldownStretchesFor()` in `training.js` compose a temperature-raise + 3 dynamic moves before, and 3 muscle-matched static holds after, woven into the existing `sessionExForProfile()` equipment-pool pipeline (so the daily seeded-variety and equipment-fallback mechanisms apply to stretches too, for free). Session 5 was rebuilt to source its flexibility block from the SAME library (`flexFromLibrary` flag) rather than a third hand-copy — literally the "stretches double as flexibility work" ask. Caught and fixed a real bug of my own making before shipping: prepending warm-up entries shifted every session's `alt` pool slot-indices out of alignment with their `bw`/`gym` arrays (recomputed all of them by hand for s1/s3/s4) and broke the Run session's `pickRunIndex()`-based variant lookup (which assumed raw array-position indexing) — fixed by switching that lookup to match on the stable `_slotIdx` metadata instead of array position, which is robust regardless of what gets prepended/appended around the working set. Verified via direct function calls (pool composition, phase tagging, alt-index alignment, equipment gating on stretches like the band-only dislocate) and real UI clicks (reference library's Warm-up/Session/Cool-down grouping, the coach card's same grouping — had to force a non-AFT-circuit day via a throwaway gym-access pattern since today's real date happened to land on the AFT day's early-return branch — and the swap UI still resolving correctly with the new phase-tagged exercises). Full ship checklist passed clean, including `--shot`. |
| 2026-08-12 | v179 (FM/test-features workstream, Phase FM-2: equipment inventory + exercise-pool tagging, idea #3 — scope expanded live to include swim/climb optional sessions, real-world-grounded equipment research, and a first weight-suggestion step) | 2026-08-12 19:36 EDT | 2026-08-12 20:11 EDT | ≈35m | **Continuous with the v178 row above.** Started as the planned FM-2 (equipment inventory + exercise tagging), then grew substantially through live user requests mid-build: (1) asked whether to build the full graded-equipment-matrix spec as originally designed vs. a right-sized two-profile version — user chose the full spec; (2) asked for real day-to-day exercise variety, not just equipment fallback, plus swim/rock-climbing session support, plus asked me to research what a Wake Forest ROTC student would actually have access to; (3) asked for adaptive rep/set/weight suggestions that learn from logged difficulty over time. Handled each honestly rather than quietly cutting scope or fabricating: researched Wake Forest's real Wellbeing Center facilities (confirmed weight room, pool, climbing wall) via web search before adding those equipment tags; asked Wyatt directly for his battalion trailer's actual contents rather than inventing one (he confirmed AFT-kit/water-jugs/weighted-stretcher, and explicitly OK'd common-PT-gear placeholders flagged as unverified for the rest); built real multi-variant exercise pools per slot (not just a bw/gym pair) so equipment-fallback and daily variety are the same mechanism, with a suggest-then-swap UI; added Swim/Rock Climbing as new opt-in, equipment-gated, coach-suggested (never auto-scheduled) session types. The adaptive-learning ask was deliberately **not** built blind — it's a genuinely large, undesigned capability (needs the same small-N-honesty design pass X-Insight already calls for), so only a grounded first step shipped (a real last-logged-weight suggestion in `prescriptionFor()`, sourced from actual `S.workouts` history) and the full ask was written up as a new deferred phase, **FM-Adapt**, in `IDEAS-tests-fm-workouts.md`, rather than half-built under time pressure. Verified extensively beyond the standard regress suite: pool selection across rich/empty/trailer-only equipment sets, the equipment-profile UI (switch/create/rename/delete/tag-toggle), optional-session opt-in gating, the weight-suggestion text with and without logged history, and the exercise swap+override UI end-to-end (opened the alt panel, picked a non-suggested variant, confirmed `S.exChoice` was set) — all via throwaway Playwright scripts, deleted after use. Full ship checklist passed clean, including `--shot`. |
| 2026-08-12 | v178 (FM/test-features workstream, Phase FM-1: gym-access-aware weekly training scheduling + guided mock-AFT walkthrough, ideas #2/#2b) | 2026-08-12 15:36 EDT | 2026-08-12 19:32 EDT | ≈3h56m | **Continuous with the v177 row above**, but the conversation was auto-summarized (context compaction) partway through — real active work time is very likely well under the full elapsed span; logging the full span honestly rather than guessing at the split, since the summarization boundary isn't independently timestamped. By far the largest phase in this workstream so far: replaced the entire fixed `WEEK_PLAN` schedule in `training.js` with `assignWeekSessions()` (dynamic gym-day-aware session assignment, 3-layer input cascade: live same-day override → confirmed per-week pattern → saved default pattern) and `pickAftMode()` (reuses `recoveryReadiness()` + AFT history/test-date signals to decide mock/practice/circuit, no new tracking). Built the full guided mock-AFT walkthrough (`openMockAft`/timer/step functions in `aft.js`, new `#mockAftModal`) — deliberately does NOT save an `S.aft` history entry for single-event "practice" mode, since a one-event score read as a full "AFT total" would be a faked metric. Caught and fixed a wiring mistake before shipping: the mock-AFT buttons live inside a string that gets folded into a larger `innerHTML` blob in `today.js`, so `id`+`.onclick` wiring silently no-ops — converted to `data-mockaft` + the existing global click-delegation handler in `awards.js`, the same pattern already used for `data-gototab`/`data-cndel`/etc. Verified functionally beyond the standard regress suite: a throwaway Playwright script exercised zero-gym-day, all-seven-day, and same-day live-override patterns, the gym-access UI (toggle/confirm/save-default), a full 5-event mock AFT run (confirmed it pushes one real `S.aft` entry), practice mode (confirmed it does NOT push an entry), and the timer's start/stop/elapsed mechanics on a timer-bearing event — deleted afterward per the repo's scratch-script convention. Full ship checklist passed clean, including `--shot` since the weekly plan render layout changed. |
| 2026-08-12 | v177 (FM/test-features workstream, Phase X-AAR: After-Action Review journal) | 2026-08-12 15:26 EDT | 2026-08-12 15:36 EDT | ≈10m | **Directly measured, continuous with the v176 row above.** Second phase of the confirmed build order, started immediately after the v176 declutter per "begin on the next thing." Fast because the design closely reused the existing counseling log's pattern end-to-end (render function, delegated-click delete handler in `awards.js`, `SECTIONS` JSON export config, CSV export) rather than inventing a new one — the doc's own scope note ("mostly a new data model + form UI, no complex algorithm") held exactly true. The one genuinely new piece was the contextual Dawn-tab nudge (broken streak / below-standard AFT, suppressed once a recent AAR exists), reusing conditions already computed elsewhere in `today.js`. Verified through the real UI via Playwright (not just internal function calls) — had to first learn/discover that `records` sits behind the desktop sidebar's collapsed "More" section, same as any other secondary tab. Full ship checklist passed clean, no regress-hang recurrence. |
| 2026-08-12 | v176 (declutter: remove the milestone bar/quest-due-soon/AFT-date Field Notes rows superseded by v175's Upcoming card; trim qual/counseling alerts to overdue-only) | 2026-08-12 14:29 EDT | 2026-08-12 15:26 EDT | ≈57m | **Directly measured, continuous with the v175 row above.** Wyatt asked to resolve the duplication v175 flagged as an open decision rather than deciding it. Removed the pieces Upcoming fully superseded (all forward-looking-only); trimmed — not removed — the qualification/counseling alerts, since those covered overdue cases Upcoming deliberately excludes by design (that's the overdue-oaths count / Warrior's Focus's job). Deleted the now-dead milestone-bar CSS after confirming via grep it wasn't used anywhere else (the separate milestone-*list*-management CSS elsewhere was left alone — different UI, same word). Verified with a functional Playwright smoke test seeding one future + one past item in both quals and counseling, confirming the overdue alert fires only for the past item and the milestone bar is gone. Full ship checklist passed clean on the first attempt this time — no regress hang recurrence. |
| 2026-08-12 | v175 (FM/test-features workstream, Phase X-Timeline: unified "Upcoming" cross-tab deadline view) | 2026-08-12 12:21 EDT (approx. — overlaps the tail of the v174 row above, not a clean boundary) | 2026-08-12 14:29 EDT | ≈2h08m | **Directly measured, though the start timestamp is shared/approximate with the v174 row's own end** (continuous session, no clean cutover). Opened the confirmed `planning/IDEAS-tests-fm-workouts.md` build order and started Phase X-Timeline (first in the confirmed order, zero dependencies, no open design questions) directly — no re-audit round, per the lesson from the GUI-revamp redundancy. Found the doc's original 2026-07-03 "no merged view anywhere" premise had partially eroded since — `today.js` had grown several individual scattered date indicators (milestone bar, qual/counseling alerts, quest-due-soon note, AFT-test-date note) by now; built the new unified `renderUpcomingTimeline()` as pure additive aggregation per the phase's actual scope, deliberately leaving the pre-existing scattered ones in place rather than unilaterally removing already-shipped UI — flagged the resulting duplication as an open decision rather than deciding it alone. **Real environmental problem, not a code bug:** `npm run regress` hung repeatedly waiting on `networkidle`/`load`; diagnosed by loading the last-committed, already-shipped `index.html` standalone and reproducing the identical hang, proving it was system memory pressure (4.6GB free of 31.5GB), not this session's diff. Surfaced to Wyatt directly via `AskUserQuestion` rather than guessing at a fix; he freed memory, one retry still transiently timed out, the next passed clean (`PAGEERRORS 0`, full skill audit clean). Skipped an optional visual screenshot afterward given the system was still under load and it wasn't part of the actual required verification. Full ship checklist otherwise passed clean. |
| 2026-08-12 | v174 (GUI revamp session 2: mobile bottom-bar + drawer nav, light visual polish) + backfilled v168–v171 into this log and `FINISHED-FEATURES.md` | not recorded | 2026-08-12 12:21 EDT | not measured (multi-day gap, no start timestamp taken) | **Not directly measured** — same continuation-after-a-gap situation as the v173 row above (conversation spanned multiple real days; no `date` taken at the true start of this sub-session). Two distinct pieces of work: (1) backfilled v168–v171's real history into both planning docs after Wyatt caught that the v172 session had partly re-audited work those versions already did — see the backfill notes inline in this file and in `FINISHED-FEATURES.md`'s v168–v171 entries; (2) shipped v174 itself. Confirmed via `AskUserQuestion` before implementing: light visual polish only (not a redesign), mobile nav becomes a bottom bar + drawer (not a grouped scroll strip), roughly-even priority across visual/nav/mobile. Real bug caught via screenshot, not assumed: the first CSS pass for the mobile bar/drawer used plain class selectors that lost to the base `nav.tabs`/`nav.tabs button` rules on specificity, rendering as a vertical stack of oversized cards instead of a bottom bar — fixed by bumping to compound `nav.tabs.mobile-bar` selectors, re-verified with fresh screenshots and a full Playwright functional pass (bar tap, drawer open, drawer-tab tap + auto-close, backdrop-tap close) at a 375px viewport. One `AskUserQuestion`-gated edit to `FINISHED-FEATURES.md` was denied once mid-session by the permission layer (not content-related) and succeeded on retry with identical content. Full ship checklist passed (`build`/`check`/`regress`/`package`, zero page errors). Flagged but deliberately not fixed: `_shell.html`'s Google Fonts CDN link is a real pre-existing violation of the project's own "no external fonts/CDNs" hard rule — fixing it means vendoring the same fonts locally, judged out of "light polish" scope without a separate decision from Wyatt. |
| 2026-08-10 | v173 (TOC data bridge — cross-project, spans this repo + the separate TOC repo) | not recorded | 2026-08-10 11:28 EDT | not measured (multi-day gap) | **Not directly measured** — this request arrived as a follow-up in the same conversation as v172, but the conversation resumed after a multi-day gap (date changed 2026-08-07 → 2026-08-10 mid-session) with no `date` timestamp taken at the actual start of TOC investigation, so elapsed time here isn't comparable to other rows (real idle time between messages is folded in, unknown how much). Logged as a start-time gap per this file's own stated convention rather than guessed. Work itself: investigated a separate project (TOC, a personal offline project-launcher app) via two research agents before writing any code — found its static host is deliberately read-only and it never mutates other projects, surfaced the tension to Wyatt via `AskUserQuestion` before picking a mechanism, then discovered mid-implementation that the originally-agreed `pywebview` bridge approach needed a `postMessage` relay (iframe-based tabs, not separate windows) and flagged a simpler alternative (TOC's backend is plain HTTP/fetch already) back to Wyatt rather than silently substituting it. Implemented on both sides: TOC gained an opt-in `data_bridge` registry field + a new `projectdata.py` module + two CORS-scoped routes (243 tests passing there after, including a caught write-path-allowlist meta-test and a live-verified CORS wrinkle on `/api/health`); Operations gained a `tocInit()`/`tocWriteDebounced()` bridge in `app-setup.js` alongside the pre-existing cloud-file-sync. Full ship checklist passed on Operations' side (`build`/`check`/`regress`/`package`); TOC's own `pytest`/`ruff`/`mypy` all clean. **Update (same day, folded into this row rather than a new one since it was a direct continuation):** the live end-to-end check did happen — Wyatt closed his running TOC instance to let it be tested fresh, a real TOC session was started, Operations opened through its actual static host, a change was made and confirmed to survive a full page reload via the bridge, then all test artifacts (fake save data, the TOC instance started for the test) were cleaned up. See the v173 entry in `FINISHED-FEATURES.md`. |
| 2026-08-07 | v171 (remove duplicate Core Skills section from the skills tab) | not recorded | 2026-08-07 09:12 EDT | not measured | **Backfilled during the v173 session** — never logged at the time (this row and the three below are why `NEXT-SESSION-PROMPT.md` went stale claiming "v167 current" through v172, causing v172 to partly re-audit territory these already covered). Reconstructed from `git log`'s commit timestamp (End only — no start timestamp exists in the historical record) and the commit's own message; see the matching `FINISHED-FEATURES.md` entry for what shipped. |
| 2026-08-06 | v170 (pyramid-tree-first skills browsing: Mythic → Legendary → Rare → Uncommon → Common) | not recorded | 2026-08-06 18:08 EDT | not measured | **Backfilled during the v173 session.** ~2h39m after v169's commit same day — plausibly a continuation of the same working session, plausibly a separate one with a break; genuinely unclear from the git record alone, not guessed. Explicitly "the core piece of the GUI revamp Wyatt asked for" per the commit's own words. |
| 2026-08-06 | v169 (consolidate Habits + Daily Orders into one unified daily-tasks list) | not recorded | 2026-08-06 15:29 EDT | not measured | **Backfilled during the v173 session.** 25 minutes after v168's commit — very plausibly the same session as v168 below, not logged as such since neither was logged at the time. |
| 2026-08-06 | v168 (GUI audit fixes: Chain view, Side Deck scale, tree mastery insignia, 5 cross-tab bugs) | not recorded | 2026-08-06 15:04 EDT | not measured | **Backfilled during the v173 session.** First of the GUI-revamp-adjacent sessions after v167 — see `FINISHED-FEATURES.md` for the full "18-tab audit" scope. |
| 2026-08-07 | v172 (GUI revamp session 1: skills-tab audit + fixes — Tree view crowding, Focus strip, pyramid explainer) | 2026-08-07 09:21 EDT | 2026-08-07 09:39 EDT | ≈18m | **Directly measured, single-purpose session.** Audit-first per `IDEAS-gui-revamp.md`: read `skills.html`/`skills.js`/`tree.js`/`skills-core.js` end to end, found the concrete root cause of the "can't see skills" complaint (tree.js drawing 700-1500+ leaves per world), then confirmed design direction with Wyatt via `AskUserQuestion` before writing any code. Implementation was small and surgical once scope was confirmed: one new helper (`catProgressFraction`), one tree.js rewrite (remove leaf-swarm rendering, light up realm discs instead), one new render function (`renderFocusStrip`) plus one new helper (`skReadyToCombine`), one collapsed explainer block, targeted CSS. Full ship checklist passed clean first pass except `npm run regress -- --shot`, which hit a pre-existing unrelated selector bug in the test script itself (matched the wrong "tree" button) — diagnosed with a throwaway debug script confirming zero page errors in the real tree code, then fixed the one-line selector. Note: this session found `planning/FINISHED-FEATURES.md` and this log both had a gap between v167 and v172 (v168–v171 shipped per `git log` but were never logged in either doc) — not backfilled here, flagging for whoever next has reason to care. |
| 2026-08-06 | v167 (Commons layer, sixteenth and FINAL tree: Cyber Operator, 625 skills / 125 setKeys) | 2026-08-06 11:07 EDT | 2026-08-06 13:08 EDT | ≈2h01m | **Directly measured, single-purpose session, fresh conversation resumed from `NEXT-SESSION-PROMPT.md`.** Re-derived the pyramid state fresh at start (11899 total, 15 done, 1 remaining — Cyber Operator — matched exactly). Ran `npm run regress` first thing to close the loop on v166's unverified environment issue — passed cleanly, confirming it was transient. Mapped the tree's structure directly from `SEED_SKILLS`, staged 125 markers in one edit (a transient OneDrive file-lock error on the first write attempt was caught and retried safely via a temp-file+atomic-rename script, after confirming via `git diff` the first attempt hadn't touched the file), committed as "v167 (staged)" before dispatching any agent. Pre-identified this tree's unusually high thematic density in the scoping doc (four "vulnerability"-themed groups, two ATT&CK-mapping setKeys, two actor/attribution setKeys, two timeline-reconstruction setKeys) and wrote explicit sibling-group differentiation into every relevant agent prompt before any wave ran, rather than discovering it mid-wave. 13 sequential dispatch waves, 2 agents/wave throughout (1 solo final wave for group 25), verified after every wave. Zero defects reached the orchestrator's independent validator as real — every wave had at least one agent self-catch and fix its own mistake (marker-duplication from an overreaching Edit span, or an undercount/missing-field on a first draft) before reporting done, the first tree in this workstream where the standing per-agent self-verification instructions did all of the actual defect-catching. 5 intermediate checkpoint commits at Legendary boundaries. Full recursive whole-tree sweep confirmed Cyber Operator 5/5/5/5 clean top to bottom, zero orphans; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:12524`. **This is the sixteenth and final Mythic tree — the entire v150–v167 Commons-layer workstream is now complete.** Mid-session, a new GUI-revamp workstream was requested and captured in `planning/IDEAS-gui-revamp.md` for the next session. |
| 2026-08-06 | v166 (Commons layer, fifteenth tree: Life Architect, 625 skills / 125 setKeys) | 2026-08-06 08:56 EDT | 2026-08-06 10:53 EDT | ≈1h57m | **Directly measured, single-purpose session, fresh conversation resumed from `NEXT-SESSION-PROMPT.md`.** Re-derived the pyramid state fresh at start (11274 total, 14 done, 2 remaining — Cyber Operator, Life Architect — matched exactly). Picked Life Architect (`cat:"personal"`) over Cyber Operator per the doc's own tiebreaker (4 known dupes vs 5). Mapped the tree's structure directly from `SEED_SKILLS`, found the same benign legacy-prefix quirk as v164/v165 (5 setKeys using the old `pers_c_*` prefix from pre-existing legacy skills wired into the first Legendary), staged 125 markers in one edit, committed as "v166 (staged)" before dispatching any agent. 14 sequential dispatch waves, 2 agents/wave throughout (1 solo final wave for group 25), verified after every wave. One real content defect: Group 21's agent initially wrote 5-item ladders instead of the required 4-item shape — caught by the post-wave validator, but a dedicated fix agent found the file already self-corrected by the original agent before the fix was dispatched (a new failure-mode nuance: an orchestrator-caught defect can race against the same agent's own in-flight self-correction). 4 intermediate checkpoint commits at Legendary boundaries. Full recursive whole-tree sweep confirmed Life Architect 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Crossed 11,800 total skills this session (11274 → 11899). `npm run build` and `npm run check` passed clean, but **`npm run regress` failed to complete** — Playwright's headless Chromium could not launch in this environment (reproduced with a bare `chromium.launch()` smoke test, unrelated to any code change) — flagged in `NEXT-SESSION-PROMPT.md` for the next session to re-run in a working environment. `npm run package` succeeded (no Playwright dependency) and produced `dist/operations.zip`. |
| 2026-08-05 | v165 (Commons layer, fourteenth tree: Cognitive Athlete, 625 skills / 125 setKeys) | 2026-08-05 13:07 EDT | 2026-08-05 14:37 EDT | ≈1h30m | **Directly measured, single-purpose session — a direct continuation of the v164 session in the same conversation** ("please begin the next commons layer"), so no separate resume-doc read was needed; re-derived the pyramid state fresh anyway per standing discipline (10649 total, 13 done, 3 remaining, matched exactly). Picked Cognitive Athlete (`cat:"cognitive"`) as the lowest known-dupe-count remaining tree (1). Mapped the tree's structure directly from `SEED_SKILLS`, ran a v149-style prerequisite check, staged 125 markers, and found the same benign legacy-prefix quirk v164 first identified (5 setKeys using the old `cog_c_*` prefix from pre-existing legacy skills wired into the first Legendary). 13 sequential dispatch waves, 2 agents/wave throughout (1 solo final wave for group 25), verified after every wave. One clean interruption: wave 9's group-17 agent hit a mid-task API server error before writing anything — confirmed zero-write, redispatched cleanly, the second session in a row to hit this exact failure mode. This tree had unusually high internal thematic density (fatigue/load/offloading/reframing/pattern-recognition concepts clustering tightly) requiring explicit sibling-group differentiation in 6+ agent prompts to prevent near-miss within-tree collisions — held clean throughout. 3 intermediate checkpoint commits at Legendary boundaries. Full recursive whole-tree sweep confirmed Cognitive Athlete 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Crossed 11,000 total skills mid-session (10649 → 11274). Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:11274`. |
| 2026-08-05 | v164 (Commons layer, thirteenth tree: Staff Excellence, 625 skills / 125 setKeys) | 2026-08-05 11:30 EDT | 2026-08-05 12:53 EDT | ≈1h23m | **Directly measured, single-purpose session — a direct continuation of the v163 session in the same conversation** ("please begin the next skill group"), so no separate resume-doc read was needed; re-derived the pyramid state fresh anyway per standing discipline (10024 total, 12 done, 4 remaining, matched exactly). Picked Staff Excellence (`cat:"leadership"`) as the only remaining tree with 0 known pre-existing dupes. Mapped the tree's structure directly from `SEED_SKILLS` and ran a v149-style prerequisite check before staging 125 markers in one edit — found a pre-existing legacy-prefix quirk (5 setKeys using `lead_c_*` instead of this tree's own `lead2_c_*`, from pre-existing skills wired in per the "existing skill integration rule") but verified it was genuinely unclaimed before proceeding. 13 sequential dispatch waves, 2 agents/wave throughout (1 solo final wave for group 25), verified after every wave. One real interruption: wave 8's group-15 agent hit a mid-task API connection failure before writing anything — confirmed zero-write via `node --check` + untouched markers, redispatched cleanly with no further incident. One transient false-alarm: an independent post-wave count briefly read a setKey at 4/5 while a concurrent sibling agent was still mid-write; resolved on re-check seconds later, no fix needed — added a "re-count as your last action, after a pause" instruction to every subsequent agent prompt specifically to guard against this. 3 intermediate checkpoint commits at Legendary boundaries. Full recursive whole-tree sweep confirmed Staff Excellence 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout, despite `leadership` having 0 of its own known dupes (all risk here was from the shipped Battlefield Commander sibling). Crossed 10,500 total skills this session (10024 → 10649). Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:10649`. |
| 2026-08-05 | v163 (Commons layer, twelfth tree: Cyberspace Operations Officer, 625 skills / 125 setKeys) | 2026-08-05 08:50 EDT | 2026-08-05 10:22 EDT | ≈1h32m | **Directly measured, single-purpose session.** Resumed from a `v163 (staged)` commit left by the immediately-preceding session: 125 `// SLOT:` markers already pre-placed for `cat:"technical"`'s Cyberspace Operations Officer tree, doc'd in `IMPROVEMENTS-v163.md`. Re-verified the staged state fresh rather than trusting it (markers intact, `node --check` clean, pyramid state re-derived from `SEED_SKILLS` matching the staging doc's 9399/11-done/5-remaining exactly). 13 sequential dispatch waves, 2 agents/wave throughout (1 solo final wave for group 25), verified after every wave with `node --check` + member-count sweep + marker-count-dropped-by-10 check + duplicate-marker/header sweep + cumulative required-field validator across all previously-completed setKeys. **Zero defects found in any wave** — a first for this workstream; every prior Commons-tree session (v150–v162) had at least one self-caught or orchestrator-caught defect somewhere in its waves. Made 3 intermediate safety commits at Legendary-boundary checkpoints (after waves 5, 8, 10) rather than holding 625 skills uncommitted for the whole session. Highest-scrutiny area (`technical`'s 2 known pre-existing dupes, both landing in the "Cyber Operator" Legendary, waves 6-8) held clean under explicit per-name whole-file grep discipline. Full recursive whole-tree sweep confirmed Cyberspace Operations Officer 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Crossed 10,000 total skills this session (9399 → 10024). Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:10024`. |
| 2026-08-03 | v162 (Commons layer, eleventh tree: Master of the Mind, 625 skills / 125 setKeys) | 2026-08-03 11:15 EDT | 2026-08-03 13:22 EDT | ≈2h07m | **Directly measured, single-purpose session.** Re-derived the pyramid state from `SEED_SKILLS` at session start rather than trusting the resume doc — confirmed exactly 8774 total, 10 trees at 625 Commons, 6 at 0, matching the doc exactly (no drift). Ran the whole-file name+cat dup sweep first (10 pre-existing: 5 technical, 4 personal, 1 cognitive, 0 leadership) and picked Master of the Mind (cognitive) over the seemingly-safer leadership: cognitive's low dupe count plus zero same-cat-sibling risk (neither cognitive Mythic tree had shipped) outweighed leadership's zero known dupes but already-shipped sibling (Battlefield Commander). 13 sequential dispatch waves, 2 agents/wave throughout (last wave solo for the final group before the array's closing bracket), verified after every wave with `node --check` + member-count + marker-count + duplicate-marker-line sweep + duplicate-group-header sweep + cumulative required-field validator re-run. Zero file-corruption incidents. **One real defect caught by the independent post-wave sweep, not by the agent's own report:** wave 5 left `cog_c_proc_interference` and `cog_c_skill_transfer` at 4/5 members each despite the agent's confident self-report of 5/5 — fixed by hand-authoring the missing skill for each. Two markers had pre-existing name/setKey label mismatches in the underlying data, flagged explicitly in agent prompts and handled correctly. Full recursive whole-tree sweep confirmed Master of the Mind 5/5/5/5 clean top to bottom (5 Legendaries → 25 Rares → 125 Uncommons → 625 Commons). Whole-file dup sweep back to exactly the same 10 pre-existing pairs. Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:9399`. |
| 2026-08-03 | v161 (Commons layer, tenth tree: Warrior Foundation, 625 skills / 125 setKeys) | 2026-08-03 09:09 EDT | 2026-08-03 10:59 EDT | ≈1h50m | **Directly measured, single-purpose session.** Re-derived the pyramid state from `SEED_SKILLS` at session start rather than trusting the resume doc — confirmed exactly 8149 total, 9 trees at 625 Commons, 7 at 0, matching the doc exactly (no drift). Picked Warrior Foundation (tactical) as the lowest-collision-risk of the 7 remaining trees and the first second-gen Phase 7 tree to get Commons. 13 sequential dispatch waves, 2 agents/wave throughout (last wave solo for the final group in the file), verified after every wave with `node --check` + member-count + marker-count + duplicate-marker-line sweep + duplicate-group-header sweep + cumulative required-field validator re-run. Zero file-corruption incidents. **New failure mode found and fixed**: wave 1 introduced 3 name+cat collisions against the already-shipped legacy Tactical Mastery tree (same `cat:"tactical"`, different Mythic tree) — not covered by the known 10-item pre-existing dupe list. Fixed by renaming the 3 new skills; strengthened every subsequent agent prompt with an explicit per-name collision grep, which then self-caught several more near-misses before they were ever written. Full recursive whole-tree sweep confirmed Warrior Foundation 5/5/5/5 clean top to bottom, all 9 previously-completed trees untouched at 625 each, all 6 remaining trees untouched at 0. Whole-file dup sweep back to exactly the same 10 pre-existing pairs after the fix. Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:8774`. |
| 2026-07-31 | v160 (Commons layer, ninth tree: The Living Root, 625 skills / 125 setKeys) | 2026-07-31 10:36 EDT | 2026-07-31 12:29 EDT | ≈1h53m | **Directly measured, single-purpose session.** Re-derived the pyramid state from `SEED_SKILLS` at session start rather than trusting the resume doc — confirmed exactly 7524 total, 8 trees at 625 Commons, 8 at 0, matching the doc exactly (no drift). Ran the whole-file name+cat dup sweep first and picked The Living Root (roots) as the lowest-collision-risk of the 8 remaining trees — also the last of the original legacy-gen 16 Mythics. 13 sequential dispatch waves, 2 agents/wave throughout (last wave solo since it was the final group in the file), verified after every wave with `node --check` + member-count + marker-count + duplicate-marker-line sweep + duplicate-group-header sweep + cumulative required-field validator re-run across every previously-completed setKey. Zero file-corruption incidents. Found one pre-existing unrelated defect (a duplicated group-header comment line in the already-shipped Soldier Athlete tree) via the routine sweep and fixed it on sight. Several agents self-caught and fixed minor mid-task slips (a duplicated header, a stray marker/comma artifact, a 4/5 undercount) via the standing re-grep/re-count discipline before reporting done — none reached the committed file uncaught. Full recursive whole-tree sweep confirmed The Living Root 5/5/5/5 clean top to bottom, all 7 previously-completed trees untouched at 625 each, all 7 remaining trees untouched at 0. Whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:8149`. |
| 2026-07-30 | v159 (Commons layer, eighth tree: Sovereign Self, 625 skills / 125 setKeys) | 2026-07-30 12:15 EDT | 2026-07-30 13:59 EDT | ≈1h44m | **Directly measured, single-purpose session.** Re-derived the pyramid state from `SEED_SKILLS` at session start rather than trusting the resume doc — confirmed exactly 6899 total, 7 trees at 625 Commons, 9 at 0, matching the doc exactly (no drift). User picked Sovereign Self (personal) from a shortlist that deliberately included the collision-flagged cats rather than avoiding them. 1 canary group, then 11 waves of 2 agents, then 1 final solo group (13 dispatch rounds total), verified after every round with `node --check` + member-count + marker-count + duplicate-marker-line sweep + full required-field validator + regression check against every previously-completed setKey. Zero file-corruption incidents. Two real defects this session — both caught by the orchestrating session itself during routine post-wave verification, not self-reported by any dispatched agent: a marker line split across two physical lines by a concurrent-edit artifact (found in a not-yet-dispatched group, flagged by an adjacent agent as out-of-scope, fixed directly before dispatch), and a duplicated group-header comment line (found the same way). Full recursive whole-tree sweep confirmed Sovereign Self 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. Full ship checklist (build/check/regress/package) passed clean; `npm run regress` confirmed `PAGEERRORS 0`, `badCount:0`, `total:7524`. |
| 2026-07-29 | v158 (Commons layer, seventh tree: Soldier Athlete, 625 skills / 125 setKeys) | 2026-07-29 19:05 EDT | 2026-07-29 21:09 EDT | ≈2h04m | **Directly measured, single-purpose session.** Re-derived the pyramid state from `SEED_SKILLS` at session start rather than trusting the resume doc — confirmed exactly 6274 total, 6 trees at 625 Commons, 10 at 0, matching the doc exactly (no drift this time). User picked Soldier Athlete (physical) from a shortlist that excluded the 3 cats with known pre-existing name+cat duplicates. 13 sequential dispatch waves, 2 agents/wave throughout, verified after every wave with `node --check` + member-count + marker-count + duplicate-marker-line sweep + full required-field validator (via a small reusable Node script written this session), plus periodic cumulative re-checks of all previously-completed setKeys to catch any lost-update regression. Zero file-corruption incidents. Real issues caught pre-commit: two agents independently found and self-corrected real name-collision risks via their own pre-write grep before writing (not shipped as defects); several agents across different waves hit a transient marker-duplication artifact under concurrent editing, self-caught every time by the standing "re-grep after every Edit call" instruction — confirmed zero survived into the file via an end-of-session `sort \| uniq -d` sweep. Full recursive whole-tree sweep confirmed Soldier Athlete 5/5/5/5 clean top to bottom; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs throughout. |
| 2026-07-09 | v157 (Commons layer, sixth tree: Scholar-Warrior, 625 skills / 125 setKeys) | 2026-07-09 09:10 EDT | 2026-07-09 11:32 EDT | ≈2h22m | **Directly measured, single-purpose session.** Started by re-deriving the pyramid state from `SEED_SKILLS` rather than trusting the resume doc's "5 trees remain" claim — found the real number was 11 (the doc undercounted by omitting the 6 second-gen Phase 7 trees from its own next-steps summary despite naming them elsewhere). User picked Scholar-Warrior (academic) from a shortlist. 13 sequential dispatch waves, 2 agents/wave throughout (continuing the v154 concurrency-reduction discipline from wave 1, not scaling up mid-session), verified after every wave with `node --check` + member-count + marker-count + full required-field validator. One real defect: a missing trailing comma at the wave-8/wave-9 group boundary (last object of "Western European Languages" wasn't comma-terminated before the "Romance Languages" comment header) broke `node --check` — caught immediately by the post-wave syntax check, fixed with a one-line Edit, and named as an explicit failure mode in every subsequent wave's agent prompts with zero recurrence. No account-wide session-limit interruption, no file corruption. Full recursive whole-tree sweep confirmed Scholar-Warrior 5/5/5/5 clean; whole-file dup sweep stayed at exactly the same 10 pre-existing pairs. |
| 2026-07-08 | v156 (FM plan: warm-up/cool-down stretches for Sessions 1, 3, 4) | not recorded — a natural continuation of the v155 conversation, no clean session-start moment | 2026-07-08 18:33 EDT | not measured | **Timestamp not properly logged at start — a real gap in following the process this time.** Triggered by user feedback after an actual gym session: reported the FM tab had no before/after stretches and suspected a bug. Investigated and found it was a genuine content gap (not a rendering bug) — Sessions 1/3/4 never had warm-up/stretch entries in `SESSIONS`, unlike Session 5. User confirmed wanting real content added (not just a copy fix). Added a warm-up + 3 session-appropriate cool-down stretches to both `bw`/`gym` variants of Sessions 1, 3, 4, reusing Session 5's existing exercise name strings so `exHowto()`'s substring matching resolves correct how-to text with zero new lookup entries. Updated `plan.html` write-ups and glossary to match. Unrelated to the SEED_SKILLS pyramid workstream — separate data structure (`SESSIONS`), no `SKILL_LADDER_VER` bump needed. |
| 2026-07-05 | v155 (retroactive audit + fix pass — no new tree) | 2026-07-05 19:56 EDT | 2026-07-05 20:38 EDT | ≈42m | Prompted by a direct question about whether earlier trees got the same scrutiny as the two most recent ones. Ran the v153-introduced required-field validator retroactively against v150–v154 (never done before) and found real defects: 1 trivial `fadeDays` typo in Tactical Mastery (v152), and 74 array-length defects (48 skills, 10 setKeys) in Keeper of the Flame (v151) where `roadmap`/`maintain` had 2-3 items instead of 4 — invisible to the member-count/marker-count sweeps used at the time. Also ran a broader tier-agnostic why/howTo/levels presence check across the whole file (not just Commons) and found 12 pre-existing Uncommon/Rare skills missing `howTo` entirely, predating the Commons pipeline (some tagged `// v120`). Fixed all of it: 2 focused agents for the Keeper of the Flame array-length fixes (verified no cross-clobbering via a full re-scan), hand-authored `why`/`howTo` for the 12 legacy gaps. Re-verified all 5 Commons trees fully clean, all 16 Mythic trees' 5/5/5 Uncommon-layer claim still holds, whole-file dup count still exactly 10, total skill count unchanged at 5649. `SKILL_LADDER_VER` bumped to 117 since existing guidance text changed. **Lesson: a validator introduced partway through a workstream doesn't retroactively apply itself — worth periodically re-running newer checks against older, already-shipped work, not just going forward.** |
| 2026-07-04/05 | v154 (Commons layer, fifth tree: Vital Operator, 625 skills / 125 setKeys) | 2026-07-04 12:52 EDT | 2026-07-05 19:56 EDT | ≈31h wall-clock, but NOT a real per-tree measurement — see notes | **Directly measured start/end, but elapsed time is dominated by a long user-side pause during incident recovery (checking OneDrive version history), not active work — treat active-work time (rough estimate below) as the real signal, not the wall-clock span.** Also discovered mid-session that v153 (Battlefield Commander) had never actually been committed to git — it existed only in the uncommitted working tree alongside all of this session's work, which is exactly what made the incident below so dangerous (no git safety net for either v153 or v154 until this session's closing commit). Sequence: 7 dispatch waves (waves 1–3 at 4 agents/wave with zero incidents; wave 3 had one sub-incident — all 4 agents hit a simultaneous API/stall failure mid-wave, recovered via a canary agent + re-dispatch of the 3 unfinished groups, no data lost). **Wave 4 hit two serious incidents**: (1) a script fallback used `open(path,'w')` (truncating write), which zeroed `skills-data.js` to 0 bytes when a race with another concurrent agent caused it to fail before writing replacement content — recovered via OneDrive version history (a snapshot from ~23 minutes earlier that, by chance, captured the state almost exactly at the interruption point, verified byte-for-byte clean via the full required-field+member-count+dup sweep); (2) even after switching every subsequent agent to a safer "read fresh → write to temp → atomic rename" pattern, a *lost-update* race still silently reverted one already-completed setKey (`phys2_c_self_massage`, 5 skills) back to its marker — atomic rename prevents torn/truncated writes but not clobbering another agent's concurrent edit with a stale full-file snapshot. Caught by the routine post-wave member-count sweep (member count went from 5 to 0 for that one setKey), fixed by hand-authoring the 5 skills directly rather than redispatching an agent. From wave 5 onward, reduced concurrency to 2 agents/wave and explicitly instructed every agent to prefer the Edit tool (which has live staleness detection) over any script fallback — zero further incidents across waves 5, 6, and 7. **Rough active-work-time estimate** (summing reported individual agent durations plus verification/investigation time, excluding the OneDrive-wait pause): likely 3–4 hours, not 31. **Lesson for future sessions:** atomic rename is necessary but not sufficient for concurrent shared-file writes — it only prevents the file being observed in a torn/truncated state, not a full-file "lost update" when two agents' read-modify-write cycles overlap. The only way to make the write itself race-safe (not just crash-safe) is either serializing writers with a lock, or preferring the harness's own Edit tool (which does real staleness detection against the live file) over any home-rolled read/write script. Also confirmed: git commit history is a genuine safety net that this project had NOT been using between sessions — v153 and v154 both existed only in the uncommitted working tree until this session's closing commit, which is exactly why the incident above required OneDrive recovery instead of a trivial `git checkout`. |
| 2026-07-04 | v153 (Commons layer, fourth tree: Battlefield Commander, 625 skills / 125 setKeys) | 2026-07-04 11:09 EDT | 2026-07-04 12:47 EDT | ≈1h38m | **Directly measured, single-purpose session.** Resumed from a clean v152 baseline. 7 sequential dispatch waves (4 agents/wave, last wave 1 agent). Two real defects caught by verifying after every wave rather than at the end: (1) two sibling agents in wave 1 independently used the identical name "Position of Attention Precision" for two different setKeys — caught by the standard whole-file duplicate sweep, fixed by renaming one; (2) the SAME wave-1 agent (Physical Bearing Standards) omitted the `howTo` field from all 25 of its skills despite it being required — caught only because a full per-field validator (not just member-count) was run after wave 1, not just member counts/marker counts. Fixed by hand-authoring 24 missing `howTo` values via a small script. Added a full required-field/array-length validator to the post-wave checklist from this session onward — a pure member-count+marker-count sweep would have shipped the missing-`howTo` defect silently. No account-wide session-limit interruption this session. |
| 2026-07-01/02 | v145–v151 (structural repair, Phase 7 structure/Uncommons, Commons prerequisite, Commons trees 1–2) | 2026-07-01 16:01 | 2026-07-02 13:50 | ≈21h49m | **Inferred from git commit timestamps, not directly measured.** Spans multiple unrelated versions bundled into one commit (v145–v148 aren't Commons-tree work at all), and the window likely includes non-working time (e.g. overnight). Treat as rough historical context only, not a real per-tree measurement — this row exists so the log isn't empty, not as a data point to extrapolate from. |

*(Convention: most-recent-first (descending by version number) — this file was reordered to that convention as part of the v192 cleanup pass, after v163–v167 and v172 had drifted out of sequence from being appended rather than inserted. Add new rows at the top, right below the table header.)*
